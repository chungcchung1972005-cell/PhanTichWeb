# Changelog — ALOHA Baby

Nhật ký chi tiết từng lượt thay đổi (KHÔNG nằm trong `rules/` để không bị tự nạp vào mọi phiên; chuyển nguyên văn từ `.claude/CLAUDE.md` ngày 2026-09-25 để CLAUDE.md gọn hơn). CLAUDE.md chỉ giữ bản tóm tắt; đọc file này khi cần biết **vì sao** một thứ được làm như vậy, lỗi nào đã gặp, test nào kiểm tra nó.

**Cách ghi tiếp:** mỗi lượt thay đổi đáng kể thêm 1 mục MỚI LÊN ĐẦU phần "Nhật ký" (ngày, yêu cầu của người dùng, đã làm gì, lỗi phát hiện kèm, file test). Đồng thời cập nhật tóm tắt 1 dòng trong CLAUDE.md nếu là tính năng/quyết định mới.

## Nhật ký (mới nhất ở trên)

- **"Ảnh của tôi": nút "Chọn tất cả" / "Bỏ chọn tất cả" dưới bộ đếm "Đã chọn" (2026-09-26):** người dùng yêu cầu thêm nút chọn/huỷ tất cả, tự thiết kế sao cho hợp trải nghiệm khách. Hai nút dạng pill nằm trong ô `#psSummaryCard`: "Chọn tất cả" viền hồng, "Bỏ chọn tất cả" màu trung tính. Gọi là "Bỏ chọn" thay vì "Hủy" để không bị hiểu nhầm là huỷ đơn. **Quyết định thiết kế vì tiền:** gói chỉ có 10 ảnh miễn phí, album 192 ảnh nên chọn tất cả = 182 ảnh vượt gói = 9.100.000đ. Vì vậy "Chọn tất cả" LUÔN mở hộp xác nhận (`#psSelectAllModal`, dùng chung khung `.ps-modal`) ghi tổng số ảnh, số ảnh tính phí và chi phí dự kiến; nút được focus sẵn là "Để mình tự chọn"; Esc hoặc bấm ra ngoài thì huỷ. Ảnh chọn trước vẫn được ưu tiên vào gói, từ ảnh thứ 11 mới đánh dấu `extraPhotos`. "Bỏ chọn tất cả" không hỏi lại mà hiện dòng "Đã bỏ chọn X ảnh · Hoàn tác" (tự ẩn sau 8 giây, mất khi khách tự chọn ảnh khác để không ghi đè lựa chọn mới); Hoàn tác khôi phục đúng cả ảnh tính phí. Chỉ tác động lên ảnh đang hiện theo tab (tab Yêu thích thì "Chọn tất cả" bị khoá). Cả 2 nút bị khoá khi đang chờ thanh toán QR hoặc đã gửi yêu cầu (`syncBulkButtons()` gọi từ `updateSummary`, `applyFilter`, `lockSelection`, `executeSubmit`). `.ps-summary` rộng tối thiểu 320px để 2 nút nằm cùng hàng (màn 320px tự xuống 2 hàng). `.ps-head-row` đổi canh đáy -> canh đỉnh, vì ô "Đã chọn" cao lên sẽ đẩy tiêu đề "Ảnh của tôi" xuống thấp. Test mới `_screenshots/test-bulk-select.js` (29 case, gồm bố cục 320-1440px); 12 file test offline đều pass. **Lưu ý cho lần sau:** công cụ Write ghi file theo đường dẫn Unicode dạng dựng sẵn (NFC), trong khi thư mục dự án thật tên dạng tổ hợp (NFD, "Thư mục mới (2)"), nên nó tạo nhầm một thư mục song song cùng tên trong C:\Users\DELL. Đã chuyển file về và xoá thư mục thừa. Muốn ghi file vào repo thì ghi ra scratchpad rồi dùng node/Copy-Item với đường dẫn tương đối.
- **Lightbox "Ảnh của tôi": bấm vùng tối quanh ảnh để đóng (2026-09-26):** người dùng muốn bấm vào toàn bộ vùng nền tối quanh ảnh (không chỉ nút X) cũng thoát chế độ xem ảnh để lướt ảnh khác. Trước đó chỉ vài dải mép màn hình đóng được, vì phần lớn vùng tối thuộc `.ps-lb-stage` (khung chứa ảnh + nút ‹ ›). Đã thêm vào `js/chon-anh.js`: bấm vào `.ps-lb-stage` ngoài ảnh/nút thì đóng, bấm chỗ trống của thanh trên cùng (kể cả ô đếm ảnh, trừ các nút) cũng đóng. **Hai điểm dễ sai:** (1) trình duyệt vẫn phát `click` sau khi kéo/vuốt/chụm 2 ngón, nên dùng cờ `gestured` (di chuyển > 6px hoặc 2 ngón) để không đóng nhầm khi vuốt chuyển ảnh hay kéo ảnh đang phóng to. (2) Do `setPointerCapture`, click có thể rơi vào chính `.ps-lb-stage` dù người dùng bấm trên ảnh, nên xét nơi BẮT ĐẦU chạm (`downOnBackdrop`) thay vì `e.target`. CSS: nới vùng bấm quanh nút ‹ › 12px (`::before`) và quanh nhóm nút phóng to 8px (padding + margin âm) để bấm hơi lệch không bị đóng; nền dùng con trỏ thường, ảnh dùng `zoom-in`. `test-lightbox.js` thêm 7 case (33 case, đều pass). Đã kiểm thêm bằng chạm thật (CDP touch) trên điện thoại: chạm ảnh không đóng, vuốt chuyển ảnh không đóng, chạm vùng tối trên/dưới ảnh thì đóng. Lưu ý khi viết test chạm: Chrome bỏ qua cú chạm đến ngay sau một cú vuốt giả lập rất nhanh (không phát `click`), nên cần chờ khoảng 2s giữa vuốt và chạm; đây là hành vi của trình duyệt, áp dụng cho mọi nút.
- **"Ảnh của tôi": bỏ khung "Ghi chú riêng cho từng ảnh đã chọn" + sửa 2 lỗi lightbox (2026-09-26):** người dùng muốn gọn trang. Đã bỏ khung danh sách (mỗi ảnh đã chọn 1 dòng thumbnail + ô ghi chú) khỏi `index.html`, cùng hàm `renderPhotoNotes()` trong `js/chon-anh.js` và CSS `.ps-photo-note-row`. Ghi chú riêng từng ảnh giờ **chỉ nhập trong lightbox** (bấm ảnh → "Chọn làm yêu thích" → ô "Ghi chú chỉnh sửa cho ảnh này"). Dữ liệu gửi cho Thợ ảnh không đổi (`photoNotes`, `photos[].note`). Ô "Ghi chú chỉnh sửa chung cho cả bộ" vẫn giữ ở tab Yêu thích. Khi test phát hiện và sửa 2 lỗi thật của lightbox: (1) lúc đang mờ dần khi đóng (0,25s), lightbox vô hình nhưng vẫn chặn cú bấm ngay sau đó; đã thêm `.ps-lightbox:not(.show) * { pointer-events: none }`. (2) Đang gõ ghi chú ảnh A mà bấm Esc rồi mở ảnh B thì ô ghi chú vẫn hiện chữ của A, gõ tiếp sẽ lưu nhầm vào B (`syncLightbox` bỏ qua khi ô đang focus); `showLbPhoto` giờ luôn nạp đúng ghi chú của ảnh mới và `closeLightbox` bỏ focus ô ghi chú. `test-photo-notes.js` chuyển sang nhập ghi chú qua lightbox. 11 file test offline đều pass.
- **Bộ test chạy được trên mọi máy + sửa các test lỗi do kỳ vọng cũ (2026-09-26):** người dùng yêu cầu "fix tất cả các lỗi để không bị xung đột". (1) Thêm `_screenshots/test-env.js` dùng chung: tự tính thư mục dự án từ vị trí file (`ROOT`, `ROOT_URL`, `ROOT_POSIX`, `fromRoot()`, `shot()`) và tự tìm Chrome/Edge (`CHROME_PATH`, ưu tiên biến môi trường `CHROME_PATH`). Cả 23 script trong `_screenshots/` đã bỏ đường dẫn ghi cứng `D:\PhanTichWeb` và `C:\Program Files\...\chrome.exe`. **Viết test mới thì dùng `require('./test-env')`, không ghi cứng đường dẫn.** (2) Sửa kỳ vọng cũ: `test-photo-notes`/`test-photos-chat` phải chuyển sang tab Yêu thích trước khi gõ ghi chú. Ô ghi chú bị ẩn ở tab Tất cả, gõ vào ô ẩn thì chữ rơi vào nút tim vừa bấm và dấu cách bấm lại tim làm bỏ chọn ảnh. Đây là lỗi của test, trang vẫn đúng. `test-photo-notes` giờ so số ảnh với album Google Photos thay vì 16 ảnh local. `test-chat-concepts` so font logo với `--font-heading` trong CSS thay vì ghi cứng "Quicksand" (site đã đổi sang Montserrat), chỉ xét logo đang hiện, và không coi `img.mark` của menu dọc quản trị là logo cũ. `test-content-pages` chờ theo điều kiện thay vì chờ cố định 150-300ms (trước đó thỉnh thoảng trượt case tìm kiếm). Kết quả: 11 file test chạy offline đều pass (`test-auth`, `test-interest-gate`, `test-photo-notes`, `test-photos-chat`, `test-ai-chat`, `test-suggestion-nav`, `test-chat-concepts`, `test-service-albums`, `test-content-pages`, `test-lightbox`, `test-qr-flow`). Chưa chạy `test-ai-faq-menu`/`test-chat-navigate`/`test-prod-chat` vì chúng gọi AI thật và tốn hạn mức, chỉ đổi đường dẫn.
- **"Ảnh của tôi": ảnh thật từ Google Photos + lightbox + thanh toán phí ảnh chọn thêm tự động qua SePay (2026-09-25):** (1) **Ảnh:** lưới tab Tất cả/Ảnh gốc hiện toàn bộ album Google Photos chia sẻ (192 ảnh, không giới hạn), dữ liệu ở `js/google-photos-data.js` do `_screenshots/fetch-google-photos.js` sinh ra (đọc khối `ds:1` nhúng trong trang chia sẻ; chạy lại khi album đổi). Bản dữ liệu "197 ảnh" của commit trước là sai: URL lấy nhầm `photoId` nên mọi link ảnh trả HTTP 400, và `js/chon-anh.js` khi đó vẫn đọc ảnh local `images/my-photos/` (chỉ có 16 ảnh). (2) **Lightbox** (`js/chon-anh.js`, CSS `.ps-lb-*` trong `css/pages.css`): bấm ảnh để xem lớn, lướt bằng nút/phím mũi tên/vuốt, thu phóng bằng nút/cuộn chuột/nhấp đúp/2 ngón, kéo để di chuyển khi đã phóng to. Có nút "Chọn làm yêu thích" dùng chung logic `toggleSelect()` với nút tim trên lưới (vẫn giữ popup 50K khi quá 10 ảnh), và ô "Ghi chú chỉnh sửa cho ảnh này" chỉ hiện khi ảnh đã được chọn, ghi chung vào `photoNotes` (khớp mục ghi chú riêng ở tab Yêu thích). (3) **Modal QR:** tiêu đề thân thiện hơn, đếm ngược 30 phút, bỏ 2 nút "Tôi đã chuyển khoản xong"/"Thanh toán sau". **Chưa thanh toán thì KHÔNG gửi yêu cầu** (trước đó đóng modal vẫn gửi với trạng thái "Chờ chuyển khoản"). Nội dung CK là mã thanh toán riêng mỗi lần (`<mã đơn>CS<số ảnh><4 ký tự ngẫu nhiên>`). Trang hỏi `server/` mỗi 4 giây, SePay báo đủ tiền là tự gửi yêu cầu tới Thợ ảnh (`paymentStatus: 'Đã thanh toán (tự động xác nhận qua SePay)'`). Trong lúc chờ tiền, danh sách ảnh bị khoá để khớp số tiền. **Thay đổi kiến trúc do người dùng chọn:** `server/server.js` không còn "chỉ phục vụ chat", có thêm `POST /api/sepay-webhook` (xác thực header `Authorization: Apikey <SEPAY_WEBHOOK_KEY>`) và `GET /api/payment-status`. Giao dịch chỉ lưu trong bộ nhớ, chưa có database. Hướng dẫn cài SePay + giới hạn: `server/DEPLOY.md`. **Người dùng còn phải tự làm:** đăng ký SePay, liên kết tài khoản ngân hàng thật, tạo webhook, đặt `SEPAY_WEBHOOK_KEY` trên Render. (4) **Sửa lỗi có sẵn trong `css/style.css`:** ở màn ≤1180px, dropdown "Dịch vụ" của menu mobile ép `visibility: visible; pointer-events: auto` nên khi menu đóng nó vô hình nhưng vẫn chặn thao tác bấm (bấm ảnh trong "Ảnh của tôi" bị nhảy về trang chủ); đã đổi thành `inherit`. Test: `_screenshots/test-lightbox.js` (26 case) và `_screenshots/test-qr-flow.js` (cần `server/` chạy local với `SEPAY_WEBHOOK_KEY=test-key-123`). Các test cũ `test-photo-notes.js`, `test-photos-chat.js`, `test-auth.js` hardcode đường dẫn máy khác (`D:/PhanTichWeb`, Chrome ở Program Files). Chạy với đường dẫn đúng thì vẫn fail 4 case, nhưng các case này cũng fail y hệt trên code TRƯỚC đợt sửa này: do 2 commit ẩn ghi chú ở tab Tất cả/Ảnh gốc, và do test còn đòi 16 ảnh local.
- **Bảng Khách hàng / CRM: avatar chữ viết tắt + làm lại bố cục hàng (2026-09-25):** người dùng gửi ảnh mẫu và yêu cầu mỗi khách có avatar tròn chứa chữ viết tắt tự sinh từ tên, màu pastel khác nhau, dòng phụ dưới tên. Đã làm trong `crm/js/admin.js` (`customerInitials`, `customerTone`, `custPerson`, `custSource`) + CSS `.cust-*` trong `css/admin.css`. **Chữ viết tắt tính lại từ tên mỗi lần vẽ bảng, không lưu riêng**, nên đổi tên là tự đổi theo. Quy tắc: chữ đầu của từ đầu (họ) + từ cuối (tên), bỏ qua từ không bắt đầu bằng chữ cái (vd "01" trong tên demo), tên 1 từ thì lấy 2 chữ đầu, giữ đúng chữ Việt (Đ, Á...). Vì vậy 7 khách demo hiện tại ("Khách demo 01"...) đều ra "KD", chỉ phân biệt bằng màu. **Màu băm theo SĐT**, không theo tên hay vị trí dòng, nên lọc/đổi tên vẫn giữ màu; 5 tông lấy đúng bảng màu badge sẵn có. **Dòng phụ suy ra từ trạng thái phễu** (Khách quan tâm/Đã tư vấn → "Khách hàng tiềm năng", Đã đặt lịch/Đã chụp → "Đang phục vụ", Hoàn thành → "Khách hàng cũ") thay vì ghi "tiềm năng" cho mọi dòng như ảnh mẫu - để không mâu thuẫn với trạng thái. Sale: dòng phụ là SĐT (cột Liên hệ gộp tên + SĐT như cũ). Kèm theo: chip nguồn khách có icon kênh, badge trạng thái có chấm màu, icon điện thoại/lịch, ô tìm kiếm/lọc có icon, dòng "Không có khách hàng nào khớp bộ lọc" khi lọc ra rỗng, tên/SĐT/ghi chú được escape HTML. **Không làm nút "..." cuối hàng** như ảnh mẫu vì chưa có thao tác nào (xem/sửa hồ sơ) để gắn vào, tránh nút chết. Đã kiểm: 9 case quy tắc chữ viết tắt, 10 case giao diện (avatar, màu, dòng phụ, lọc giữ màu, Sale, mobile 390 không tràn ngang, không lỗi JS) + 17 case `test-auth` - pass hết.
- **Biểu đồ chạy lại hiệu ứng mỗi lần bấm mục Dashboard / Doanh thu (2026-09-25):** người dùng yêu cầu ("tôi muốn phần chart chuyển động mỗi khi tôi click vào dashboard và doanh thu"). Trước đó hiệu ứng chỉ chạy đúng 1 lần cho mỗi khối - xem xong rồi thì phải tải lại trang mới thấy lại. Giờ bấm mục menu là cột mọc lại từ 0, thanh phễu và thanh mục tiêu chạy lại, đường doanh thu vẽ lại từ đầu, các con số đếm lại.
  - **Cách làm - mỗi khối khai báo 2 việc thay vì 1:** `registerAnim(sectionId, wrap, reset, play)` trong `crm/js/admin.js` thay cho `onEnterView(wrap, fn)` gọi trực tiếp. `reset` đưa về trạng thái xuất phát, `play` chạy; lần đầu vẫn chạy khi cuộn tới như cũ, các lần sau do `replaySection(id)` gọi. Đăng ký gom theo id section trong `replays` nên thêm khối mới chỉ cần `registerAnim`, không phải đụng phần bấm menu.
  - **Key của registry là href thật của mục menu:** mục Dashboard trỏ `#kpi` chứ không phải `#dashboard` (đặt key theo tên hiển thị thì bấm sẽ không có gì xảy ra - đã vấp đúng lỗi này). Cả 3 khối Dashboard (5 thẻ KPI, biểu đồ cột, phễu) cùng đăng ký vào `kpi` vì 2 khối sau nằm ở section kế tiếp không có id riêng.
  - **Phải chặn transition trong lúc đặt lại** (`snapBack` + class `.anim-reset` trong `css/admin.css`, dùng `!important` để thắng cả transition của CSS lẫn transition gán inline bằng JS): không chặn thì người xem thấy cột tụt xuống, vạch rút ngược rồi mới chạy xuôi - nhìn như lỗi chứ không như chạy lại. Đặt lại xong vẫn phải đợi trọn 1 khung hình (2 lần `requestAnimationFrame`) rồi mới chạy, cùng lý do đã ghi ở phần Dashboard bên dưới.
  - **Đợi trang cuộn xong mới chạy** (`afterScrollSettles`): site đặt `html { scroll-behavior: smooth }` nên ngay sau khi bấm menu trang vẫn đang trôi tới nơi, chạy hiệu ứng lúc đó thì người dùng chỉ xem được nửa sau. Khối nào lúc đó vẫn nằm ngoài khung nhìn (vd phễu khi bấm Dashboard trên màn thấp) thì để dành, chạy khi người dùng cuộn tới - đo bằng tỉ lệ lọt vào khung nhìn, dùng đúng ngưỡng 0.15 của IntersectionObserver trong `onEnterView` để lần đầu và lần chạy lại hiểu "đã thấy" giống nhau.
  - **2 sửa nhỏ ở helper dùng chung:** `countUp` nhận token để vòng đếm cũ tự dừng khi bấm liên tiếp (2 vòng cùng ghi 1 chỗ thì con số nhảy qua nhảy lại); `drawStroke` xoá transition trước khi đưa nét về vạch xuất phát (không thì mỗi lần chạy lại nét "rút ngược" mất gần một giây).
  - **Một cái bẫy đã tránh:** bản đầu còn đặt luôn các con số về 0 trong `reset`, khiến mục Doanh thu trông như chưa có số liệu nào cho tới khi người dùng cuộn tới - trái nguyên tắc "nội dung không bao giờ để trống" của dự án, và làm 2 case test nhất quán số liệu fail. Bỏ hẳn: `countUp` vốn đã đếm từ 0 nên không cần đặt trước.
  - Bật giảm hiệu ứng thì không chạy lại gì cả, nội dung giữ nguyên ở trạng thái cuối.
  - Đã kiểm 14 case tự động cho riêng phần này (chạy lại thật sự từ 0 ở cả 2 mục, số về đúng giá trị, bấm liên tiếp 3 lần không loạn số, chạy lại nhiều lần không lệch số liệu, khối ngoài khung nhìn thì để dành tới khi cuộn tới, mục không có biểu đồ bấm vẫn bình thường, giảm hiệu ứng, không lỗi JS) + chạy lại test hiện có: 11 case Dashboard, 15 case Doanh thu, 12 case thu gọn menu, 18 case ngày hôm nay, 17 case phân quyền - tất cả pass.
- **Ngày hôm nay nổi bật hẳn trong lịch tháng (2026-09-25):** người dùng gửi ảnh lịch và yêu cầu làm nổi bật hẳn ngày hôm nay. Trước đó hôm nay chỉ được tô tròn con số, giữa một lưới 35 ô chữ nhỏ thì nhìn lướt rất dễ trượt mất. Giờ đánh dấu ở 4 lớp cùng lúc: ô đổi nền (chuyển sắc hồng nhạt xuống trắng), viền hồng 2px bao quanh ô, chấm số ngày to hơn (25 → 27px) tô nền chuyển sắc kèm bóng đổ, và nhãn chữ "HÔM NAY" ngay cạnh số ngày - có chữ thì người xem không phải đoán ý nghĩa của màu. Tên thứ ở đầu cột chứa hôm nay cũng được tô hồng để dóng mắt từ hàng tiêu đề xuống đúng ô. Bản danh sách theo ngày ở màn ≤760px làm tương tự (viền 2px + nền hồng nhạt + nhãn).
  - **Viền vẽ bằng lớp phủ tuyệt đối (`::after`), không phải `border` thật:** border sẽ cộng thêm bề dày vào chính ô đó và làm lệch cả lưới 7 cột vốn chia đều. Cùng lý do đó, ô today được nâng `z-index` để viền không bị đường kẻ của ô bên cạnh đè lên.
  - **Nhãn chữ tự ẩn khi ô hẹp (≤1100px):** đo thực tế thì dưới mức đó nhãn chiếm gần hết dòng đầu và đẩy số đếm lịch hẹn ra ngoài; ẩn nhãn thì viền + nền + chấm hồng vẫn đủ nhận ra ngay. Bản danh sách ở màn hẹp mỗi ngày một dòng riêng nên vẫn giữ nhãn.
  - **Xem tháng khác thì bỏ đánh dấu cột thứ** (kiểm bằng chính DOM xem lưới có ô today không) - tô mãi một cột sẽ thành chỉ dấu sai vì không ngày nào trong tháng đó ứng với nó.
  - Hiệu ứng vào nhẹ (viền dày 7px thu về 2px trong 0,5s) và tắt hẳn khi người dùng bật `prefers-reduced-motion`, lúc đó viền vẫn hiện đủ.
  - Đã kiểm 18 case tự động cho riêng phần này (viền/nền/chấm/nhãn đúng, dòng đầu ô không tràn chữ ở 1440/1280/1100/1024/900/800px, tô đúng cột thứ, chuyển tháng và nút Hôm nay, bản danh sách mobile, giảm hiệu ứng, không lỗi JS) + chạy lại test hiện có: 13 case lịch vừa màn hình, 16 case lịch tháng, 12 case thu gọn menu, 17 case phân quyền - tất cả pass. Xem ảnh chụp thật ở desktop 1440px và mobile 390px.
- **Lịch tháng nằm gọn trong màn hình, không phải cuộn (2026-09-25):** người dùng báo lịch bị tràn ra ngoài. Đo lại thì **không phải tràn ngang mà tràn dọc**: ô ngày 25 có 3 lịch hẹn nên tự giãn lên 224px, kéo cả hàng cao theo và đẩy cả mục lên 908px trong khi vùng xem chỉ ~865px - phần đầu và cuối tháng bị cắt. Đã sửa theo cách Google Calendar làm: khung lịch cao theo khung nhìn (`clamp(520px, calc(100vh - 215px), 820px)`), các hàng chia đều phần cao đó (`grid-auto-rows: minmax(0, 1fr)`) nên **mọi ô cao bằng nhau**, ô nào chứa không hết thì hiện nút "+N lịch nữa"; bấm nút đó mở popup liệt kê toàn bộ buổi chụp của ngày (vẫn theo thứ tự giờ), bấm tiếp một dòng là ra chi tiết như thường.
  - **Số thẻ hiện trong ô được đo chứ không chốt cứng:** `fitDayCells()` đo chỗ trống thật của từng ô rồi cắt bớt, nên tự thích nghi với mọi cỡ màn hình, với việc thu gọn cột menu, và với tháng trải 5 hay 6 hàng - thay vì chốt "hiện tối đa 2 thẻ" rồi sai ở màn khác. Ô thấp còn tự bỏ dòng tên dịch vụ (class `compact`) cho thẻ mỏng đi, nhờ vậy vẫn thấy được nhiều buổi hơn. Gắn `ResizeObserver` lên lưới để tính lại khi đổi cỡ cửa sổ hoặc thu/mở cột menu (thu menu chỉ đổi CSS, không sinh sự kiện `resize` của window).
  - **Hai lần đo mới ra công thức đúng:** bản đầu chừa 17px cho dòng "+N nữa" nhưng không trừ cả khoảng cách giữa các thẻ, và ép luôn hiện ít nhất 1 thẻ - kết quả là ở màn 1536x800 thẻ vẫn thò ra khỏi ô 7px. Sửa thành: trừ đúng chiều cao dòng "+N nữa", cho phép 0 thẻ (khi đó nút ghi "N lịch hẹn" thay vì "+N nữa"). Sàn chiều cao để 520px vì ép thấp hơn nữa thì ô không chứa nổi một thẻ, cả tháng thành một mớ nút vô dụng - màn quá thấp thì thà cuộn một chút.
  - Bản danh sách theo ngày ở màn ≤760px không đụng tới: vẫn hiện đủ mọi lịch hẹn, không cắt bớt (đã kiểm).
  - Đã kiểm 13 case tự động cho riêng phần này ở 1920x1080 / 1536x800 / 1440x620 (lịch nằm gọn trong màn hình, mọi ô cao bằng nhau, không thẻ nào thò ra ngoài ô, nút "+N nữa" và popup cả ngày, thu gọn menu vẫn đúng, mobile không đổi) + chạy lại toàn bộ test hiện có (17 phân quyền, 17 menu dọc, 12 thu gọn menu, 16 lịch hẹn, 11 Dashboard, 15 Doanh thu, test-photos-chat, test-photo-notes) đều pass; 12 mốc màn hình 320→1440px không chỗ nào tràn ngang.
- **Cột menu quản trị thu gọn / mở rộng tuỳ ý (2026-09-25):** người dùng yêu cầu ngay sau khi đổi sang menu dọc. Thêm nút mũi tên kép cạnh logo: bấm là cột co từ 236px xuống 72px, giấu toàn bộ chữ (tên mục, chữ "ALOHA Baby", dòng "Xin chào") và chỉ chừa icon căn giữa; bấm lần nữa mở lại. **Thu gọn thành dải icon chứ không ẩn hẳn** để vẫn chuyển mục được ngay khi đang thu; rê chuột lên icon thì hiện tooltip tên mục (`data-label` do JS lấy từ chính nội dung mục, không khai báo tên ở 2 nơi rồi lệch nhau). Lúc thu gọn: nút Đăng xuất rút thành icon vuông (icon vẽ bằng CSS mask, không phải sửa HTML chỉ vì một trạng thái hiển thị), badge thông báo của Thợ ảnh co thành chấm nhỏ ở góc icon, vạch đánh dấu mục đang xem kéo hết chiều cao ô cho dễ thấy.
  - **Nhớ trạng thái và không bị giật khi tải lại:** lưu trong localStorage key **`aloha_admin_ui`** - đây là tuỳ chọn hiển thị, KHÔNG phải dữ liệu nghiệp vụ, không đụng tới 2 kho mô phỏng `aloha_auth` / `aloha_demo_db` nên không phạm guardrail kiến trúc. Trạng thái được khôi phục bằng một đoạn script nhỏ đặt ngay đầu `<body>` (trước khi trang vẽ) thay vì đợi `crm/js/admin.js` chạy ở `DOMContentLoaded` cuối body - nếu đợi thì menu hiện đủ rộng rồi mới co lại, thành một cú giật thấy rõ. Mọi truy cập localStorage đều bọc try/catch để trình duyệt chặn lưu trữ vẫn dùng được bình thường.
  - **Một chỗ suýt hỏng âm thầm:** biểu đồ Doanh thu vẽ viewBox bám đúng bề ngang khung chứa, mà thu/mở menu chỉ đổi CSS nên **không sinh sự kiện `resize` của window** - biểu đồ sẽ giữ nguyên kích thước cũ và méo đi. Đã đổi từ `window.addEventListener('resize')` sang `ResizeObserver` theo dõi chính khung chứa biểu đồ (vẫn giữ fallback resize cho trình duyệt không hỗ trợ). Có test kiểm đúng điều này: viewBox đổi từ 771 xuống 695 khi mở lại menu, khớp bề ngang thật.
  - **Màn hẹp không bị ảnh hưởng:** nút thu gọn bị ẩn (đã có nút menu ở thanh trên cùng) và mọi rule thu gọn bị vô hiệu hoá trong breakpoint ≤1000px, nên ngăn kéo trượt luôn mở đủ rộng kèm đủ chữ kể cả khi trạng thái đang lưu là "collapsed".
  - Đã kiểm 12 case tự động cho riêng phần này (co/mở đúng kích thước, chữ ẩn mà icon còn, nội dung giãn ra không tràn, vẫn bấm chuyển mục được lúc thu gọn, tooltip, biểu đồ vẽ lại, nhớ trạng thái sau khi tải lại, áp dụng sớm không giật, mobile không đổi) + chạy lại toàn bộ test hiện có (17 phân quyền, 17 menu dọc, 16 lịch hẹn, 11 Dashboard, 15 Doanh thu, test-photos-chat, test-photo-notes) đều pass; 12 mốc màn hình 320→1440px không chỗ nào tràn ngang.
- **Thiết kế lại mục "Lịch hẹn" thành lịch tháng có popup chi tiết (2026-09-25):** người dùng yêu cầu bỏ bảng danh sách + 3 tab Sắp tới/Hôm nay/Đã qua, thay bằng lưới ô ngày trong tháng, mỗi ô chứa lịch hẹn của ngày đó xếp theo giờ từ sớm tới muộn, bấm vào một lịch hẹn thì mở popup thông tin cụ thể (kèm 2 ảnh tham chiếu: bảng cũ và giao diện Google Calendar). Đã làm: thanh công cụ "Hôm nay / ‹ › / Tháng 9, 2026 / N lịch hẹn trong tháng"; lưới 7 cột CN→Thứ 7 có đệm ngày cuối tháng trước và đầu tháng sau cho đủ hàng (để mờ); ô hôm nay đánh dấu tròn hồng; mỗi lịch hẹn là một thẻ bấm được hiện giờ + tên khách + dịch vụ, viền trái và nền theo màu trạng thái (tái dùng đúng bảng màu badge sẵn có, không thêm màu mới), lịch "Đã hủy" gạch ngang tên. Popup dùng lại khung modal của kanban, hiện khách hàng + SĐT, giờ và thứ/ngày đầy đủ, dịch vụ, gói chụp, concept, ekip phụ trách, ghi chú; đóng được bằng nút X, bấm ra ngoài hoặc phím Esc.
  - **Ngày lưu dưới dạng độ lệch so với hôm nay (`dayOffset`), không phải ngày cố định:** dữ liệu demo nhờ vậy luôn hợp lý dù xem vào lúc nào. Bảng cũ từng ghi "09:30 · 22/09 - Sắp đến" trong khi hôm nay đã là 25/09, tức lịch quá khứ mà vẫn mang trạng thái tương lai; cách mới không mắc lại lỗi đó. Trạng thái dùng đúng enum lịch hẹn trong `rules/tech-defaults.md` và khớp vị trí so với hôm nay (đã qua → Đã chụp/Hoàn thành/Đã hủy; hôm nay → Đã đến/Đang chụp, Sắp đến; sắp tới → Sắp đến/Chờ xác nhận/Chưa cọc/Chờ duyệt dời lịch), có test tự động kiểm đúng quan hệ này. Dữ liệu tăng từ 5 lên 15 lịch hẹn để lịch tháng không trống trải, tên khách khớp `CUSTOMERS`. Vẫn là dữ liệu minh hoạ tĩnh, chưa nối `aloha_demo_db` - việc nối Sale/Lịch hẹn vào nguồn dữ liệu thật vẫn là mục 2 trong "Việc cần làm".
  - **Phạm vi:** section này vốn dùng chung `data-roles="sep sale"` nên Sale cũng chuyển sang lịch tháng (đã kiểm), không tách riêng hai giao diện. Màn ≤760px đổi sang danh sách theo ngày thay vì lưới 7 cột (mỗi ô chỉ còn ~50px thì không đọc nổi), vẫn giữ đúng thứ tự giờ sớm→muộn trong từng ngày.
  - **Lưu ý cho lần sau khi viết test Puppeteer liên quan tới font:** `await page.evaluate(() => document.fonts.ready)` làm treo phiên CDP (lệnh evaluate không bao giờ trả về, test đứng im hàng chục phút rồi chết vì `Runtime.callFunctionOn timed out`) - đã dính 2 lần. Nguyên nhân là puppeteer cố chuyển `FontFaceSet` về Node. Viết `document.fonts.ready.then(() => true)` hoặc chỉ cần chờ cố định vài trăm ms.
  - **Lưu ý cho lần sau khi viết test liên quan cuộn:** site đặt `html { scroll-behavior: smooth }` (`css/style.css:45`). Sau khi trang admin dài thêm vì lịch tháng, 2 test cũ bắt đầu fail mà KHÔNG phải do sản phẩm hỏng: (a) test bấm mục menu đo vị trí sau 600ms trong khi cuộn mượt còn đang chạy; (b) test hiệu ứng biểu đồ Doanh thu dùng `scrollIntoView()` mặc định (cũng mượt) nên section ló vào khung nhìn từ sớm giữa lúc cuộn, hiệu ứng chạy xong trước khi đo. Đã sửa test: đợi lâu hơn, và dùng `scrollIntoView({ behavior: 'instant' })` khi cần đo trạng thái giữa chừng của hiệu ứng.
  - Đã kiểm 16 case tự động cho riêng phần này (lưới đúng số ngày, thứ tự giờ trong từng ô, popup đủ trường, chuyển tháng và nút Hôm nay, trạng thái hợp lý theo mốc thời gian, bản danh sách mobile, Sale xem được, không lỗi JS) + chạy lại toàn bộ test hiện có (17 phân quyền, 17 menu dọc, 11 Dashboard, 15 Doanh thu, test-photos-chat, test-photo-notes) đều pass; đo lại 12 mốc màn hình 320→1440px, không chỗ nào tràn ngang.
- **Đổi menu khu quản trị từ thanh ngang sang cột dọc bên trái (2026-09-25):** người dùng yêu cầu trực tiếp. **Đã nêu rõ mâu thuẫn trước khi làm:** `rules/design.md` mô tả Admin dùng "top navigation bar với logo ALOHA góc trái" theo 4 ảnh tham chiếu gốc, nên đây là thay đổi ngược định hướng cũ - đã cập nhật lại tài liệu thay vì âm thầm ghi đè. Cấu trúc mới trong `crm/admin.html`: `<aside class="admin-sidebar" id="adminSidebar">` gồm logo trên cùng, 6 mục menu (mỗi mục có icon SVG line-style tự vẽ, cùng phong cách icon sẵn có), khối người dùng + nút Đăng xuất đẩy xuống đáy cột; thêm `<header class="admin-mobilebar">` (nút menu + logo) và `<div class="admin-scrim">` chỉ dùng ở màn hẹp. **Giữ nguyên `id="adminTabs"` và các thẻ `<a data-roles="...">`** vì phần phân quyền trong `crm/js/admin.js` và `_screenshots/test-auth.js` đều lọc theo đúng cấu trúc đó - không phải sửa dòng nào của phần phân quyền.
  - Màn rộng: cột menu `position: fixed` bám mép trái, cao hết màn hình, nội dung né sang phải bằng `padding-left` trên `.admin-body` (dùng padding chứ không phải margin trên body để nền trang vẫn phủ kín). Nhân tiện nới `.container` trong khu quản trị từ 1180px lên 1320px vì giờ nó nằm trong phần bên phải cột menu, để nguyên sẽ chật hơn trước.
  - Màn hẹp (≤1000px): cột menu trượt ra từ mép trái khi bấm nút menu, có lớp phủ mờ; đóng được bằng 3 cách (bấm ra ngoài, chọn một mục, phím Esc) và `aria-expanded` cập nhật theo. Mốc 1000px vì dưới mức đó, 236px dành cho menu khiến phần bảng/kanban còn quá hẹp để đọc.
  - Mục đang xem có nền hồng nhạt + vạch màu bám mép trái; badge thông báo của Thợ ảnh vẫn hiện đúng (nằm gọn bên phải trong mục, nhờ `<span>` tên mục để `flex: 1`).
  - **Lỗi có sẵn phát hiện khi đo lại toàn bộ mốc màn hình:** ở 320px (iPhone SE/5) kanban 2 cột cần 356px nên đẩy cả trang tràn ngang - không liên quan việc đổi menu, có từ trước. Đã cho kanban về 1 cột ở ≤380px; từ 381px trở lên vẫn 2 cột như cũ, không đụng tới trải nghiệm ở các máy phổ biến 390px+.
  - Đã kiểm 17 case tự động cho riêng phần này (vị trí/hướng menu, nội dung không bị che, đóng mở ở màn hẹp bằng cả 3 cách, badge, phân quyền Thợ ảnh, không lỗi JS) + chạy lại toàn bộ test hiện có (17 phân quyền, 11 Dashboard, 15 Doanh thu, test-photos-chat, test-photo-notes) đều pass; đo 12 mốc màn hình từ 320px tới 1440px, không còn chỗ nào tràn ngang.
- **Thêm hiệu ứng chuyển động cho Dashboard Sếp + sửa 1 lỗi thật của biểu đồ cột (2026-09-25):** người dùng yêu cầu 3 khối của "Tổng quan hôm nay" (5 thẻ KPI, biểu đồ "Khách hàng mới theo tuần", phễu khách hàng) cũng có hiệu ứng như mục Doanh thu vừa làm. Đã thêm: số KPI/phễu/cột đếm tăng dần từ 0, sparkline 5 thẻ KPI vẽ dần (lệch nhau 80ms), vạch màu trên đầu mỗi thẻ chạy ngang, cột biểu đồ mọc lên lần lượt trái sang phải (lệch 70ms), thanh phễu chạy ngang lần lượt từ bậc trên xuống (lệch 100ms) - tất cả chạy đúng 1 lần khi khối lọt vào khung nhìn.
  - **Gom code chung thay vì mỗi khối tự viết:** thêm 3 helper dùng chung ở đầu `crm/js/admin.js` (`onEnterView`, `countUp`, `drawStroke`) + một biến `prefersReducedMotion` duy nhất cho cả file; khối Doanh thu làm trước đó cũng chuyển sang dùng chung (bỏ `revReduce`/`revObserver` và 2 đoạn đếm số/vẽ nét tự viết riêng). Trước đó cuối file còn khai báo `const prefersReducedMotion` lần thứ hai - nếu để nguyên sẽ là lỗi cú pháp "already declared" làm chết toàn bộ `admin.js`, đã gộp về một khai báo.
  - **Lỗi thật có sẵn đã sửa - biểu đồ "Khách hàng mới theo tuần" hiển thị sai số liệu:** đo bằng Puppeteer phát hiện cả 6 cột đều cao đúng 54.8px dù dữ liệu là 38/52/45/60/71/54 và inline style ghi đúng `height: 54%...100%` - tức biểu đồ không hề phản ánh số liệu, nhìn kỹ ảnh chụp Dashboard cũ cũng thấy 6 cột bằng nhau. Nguyên nhân: `.chart-bar` nằm thẳng trong ô flex-column cao cố định 102px, tổng chiều cao (số + cột + nhãn + gap) vượt quá ô nên `flex-shrink` mặc định co cột lại cho vừa, `height:%` mất tác dụng và mọi cột hội tụ về cùng một chiều cao. Đã sửa bằng cách bọc cột trong `.chart-bar-slot` (`flex:1; min-height:0`) - phần trăm chiều cao giờ tính trên một vùng có kích thước xác định. Sau khi sửa, cột thấp nhất chỉ còn ~30px trong khung 140px nên nhìn không ra chênh lệch, đã tăng khung lên 200px cho cân với phễu bên cạnh.
  - **Một lỗi hiệu ứng tinh vi:** ban đầu cả 3 khối đều bật thẳng tới giá trị cuối không kèm chuyển động, vì Dashboard nằm ngay đầu trang nên IntersectionObserver báo ngay trong khung hình đầu tiên - trạng thái ban đầu (cột cao 0, thanh rộng 0) chưa từng được vẽ ra nên trình duyệt coi như không có gì thay đổi và bỏ qua transition. Đã sửa trong `onEnterView`: đợi trọn 1 khung hình (2 lần `requestAnimationFrame`) rồi mới đặt giá trị đích.
  - **Lưu ý khi viết test cho loại hiệu ứng này:** `waitUntil: 'networkidle0'` chờ hơn 700ms nên đo xong thì animation đã kết thúc, dễ kết luận nhầm là "không chạy" (đã mắc đúng lỗi này 1 lần). Phải dùng `domcontentloaded` rồi đo sớm, và đo bằng `getComputedStyle` chứ không đọc inline style (giá trị đích được gán ngay trong cùng khung hình, chỉ computed style mới thấy giá trị đang chuyển động).
  - Đã kiểm 11 case tự động cho Dashboard (chuyển động thật sự chạy từ trạng thái đầu tới cuối, cột đúng tỉ lệ số liệu, giảm hiệu ứng thì hiện ngay không animate, không lỗi JS); chạy lại toàn bộ test hiện có sau khi gom code chung: 15 case Doanh thu, 17 case phân quyền, test-photos-chat, test-photo-notes - tất cả pass. Mobile 360/390px: hiệu ứng chạy khi cuộn tới, không tràn ngang.
- **Thiết kế lại mục "Doanh thu" của Sếp theo bố cục dashboard analytics + hiệu ứng động cho biểu đồ (2026-09-25):** người dùng gửi ảnh tham chiếu một dashboard dark-mode (KPI có sparkline, area chart lớn có tab Revenue/Orders/Profit, donut, Monthly Goals) và yêu cầu làm tương tự kèm hiệu ứng chuyển động ở các chart. **Đã hỏi trước khi làm** vì ảnh là dark theme, xung đột trực tiếp với guardrail "không tự tạo design system khác" trong `rules/design.md`; người dùng chốt **giữ bảng màu hồng/navy sáng** và chọn 3 khối (bỏ donut): 4 thẻ KPI + sparkline, biểu đồ đường lớn + tab, mục tiêu tháng dạng thanh tiến độ. Đã thay toàn bộ khối cũ (donut 68% + 3 ô số tĩnh) bằng: (1) 4 thẻ KPI (doanh thu tháng, số đơn, giá trị TB/đơn, % đạt mục tiêu), mỗi thẻ có icon + % tăng giảm so tháng trước + sparkline tràn sát 2 mép thẻ, số đếm tăng dần và sparkline vẽ dần khi cuộn tới; (2) biểu đồ đường 9 tháng (T1-T9/2026) đường cong mượt (Catmull-Rom quy đổi cubic bezier, độ căng thấp 0.16 để không vọt quá đỉnh dữ liệu làm phóng đại số liệu) + nền gradient, có 3 tab đổi chỉ số (Doanh thu/Số đơn/Giá trị TB) vẽ lại có hiệu ứng, hover từng tháng hiện đường dóng + chấm + tooltip; (3) khối "Mục tiêu tháng 9" 3 thanh tiến độ chạy từ 0 khi cuộn tới. Toàn bộ nằm trong `crm/js/admin.js` (`REVENUE_DATA`, ~215 dòng), CSS `.rev-*` trong `css/admin.css` thay block `.revenue-*`/`.chart-donut` cũ (đã xoá).
  - **Số liệu nhất quán theo thiết kế, không viết cứng rời rạc:** mọi con số suy ra từ một `REVENUE_DATA` duy nhất - giá trị TB/đơn = doanh thu ÷ số đơn, % tăng giảm tính từ tháng liền trước, % đạt mục tiêu = doanh thu ÷ chỉ tiêu - nên không thể lệch nhau như lỗi phễu đã cảnh báo trong `rules/design.md`. Đã test tự động đúng 2 quan hệ này. Các chỉ tiêu tháng (600 triệu, 120 đơn, 400 khách) là placeholder minh hoạ, UI ghi rõ "chờ studio xác nhận" đúng `rules/tech-defaults.md`. Số 320 khách mới khớp đúng KPI "Khách quan tâm" ở Dashboard.
  - **2 lỗi thật phát hiện khi kiểm tra bằng Puppeteer (không phải đoán qua đọc code):** (a) sparkline trong thẻ KPI hụt hẳn 37px bên phải dù CSS ghi `width: calc(100% + 36px)` - nguyên nhân là reset toàn cục `img, svg { max-width: 100% }` ở `css/style.css:56` kẹp lại, phải thêm `max-width: none` mới thoát; (b) trên điện thoại biểu đồ chỉ cao 137px và chữ trục co còn chưa tới 5px không đọc nổi, vì viewBox cố định 720x300 bị trình duyệt thu nhỏ theo bề ngang - đã sửa thành viewBox bám đúng bề ngang thật của khối chứa (tỉ lệ 1:1, chữ giữ nguyên cỡ), chiều cao 250px ở màn hẹp / 300px ở màn rộng, kèm vẽ lại khi đổi bề ngang cửa sổ (bỏ qua thay đổi chỉ về chiều cao để không vẽ lại liên tục khi thanh địa chỉ mobile trượt).
  - Tôn trọng `prefers-reduced-motion`: bật giảm hiệu ứng thì nội dung hiện đầy đủ ngay, không animate. Đã kiểm 15 case tự động (render, nhất quán số liệu, animation thật sự chạy, hover, đổi tab, mobile không tràn ngang, không lỗi JS) - pass hết; chạy lại 17 case test phân quyền vẫn pass; xác nhận Sale/Thợ ảnh không thấy mục này và không sinh lỗi JS. Đã xem trực tiếp screenshot ở 360/390/900/1440px.
- **Bỏ mục "Concept" khỏi giao diện Sếp (2026-09-25):** người dùng yêu cầu gỡ hẳn section Concept trong `crm/admin.html` (5 ô ảnh concept mẫu chỉ để xem, không có thao tác quản lý nào). Đã xoá: tab nav `Concept`, cả `<section id="concept" data-roles="sep">`, và 6 rule CSS `.concept-mini*` trong `css/admin.css` (gồm 2 rule trong media query 1120px/560px) vì sau khi xoá section thì không còn nơi nào dùng - đã grep xác nhận. **Không đụng** concept bên trang khách hàng (`index.html` dùng class khác hẳn `.concept-tile`, style nằm ở `css/style.css`) và **không xoá** ảnh `images/concept-*.jpg` vì trang chủ vẫn dùng. Sếp giờ còn 6 tab: Dashboard, Khách hàng, Lịch hẹn, Ảnh & chỉnh sửa, Doanh thu, Cài đặt. Đã cập nhật `rules/design.md` 2 chỗ (danh sách phân quyền section + mục "Admin/CRM Features") vì tài liệu gốc từng liệt kê Concept là một tính năng Admin - đây là thay đổi ngược với requirement ban đầu, nếu sau này cần quản lý concept trong Admin thì phải dựng lại. Sửa `_screenshots/test-auth.js` (danh sách tab kỳ vọng của Sếp) cho khớp; chạy lại toàn bộ 17 case test phân quyền, tất cả pass. Đã xác minh bằng screenshot desktop 1440px + mobile 390px: bố cục liền mạch (Ảnh & chỉnh sửa → Doanh thu → Cài đặt), không để lại khoảng trống, không tràn ngang.
- **Phát hiện khi chạy test (ĐÃ SỬA 2026-09-26 bằng `_screenshots/test-env.js`, xem mục đầu nhật ký):** toàn bộ file `_screenshots/test-*.js` hard-code đường dẫn cũ `D:\PhanTichWeb\` (cả `BASE` lẫn nơi lưu screenshot) trong khi dự án hiện nằm ở `F:\New folder\PhanTichWeb` - nghĩa là chạy trực tiếp sẽ fail ngay, không phải do code sản phẩm. Lần này đã lách bằng cách chạy bản copy đã chỉnh đường dẫn trong thư mục tạm, không sửa file dự án.
- **Tầng nội dung chi tiết cho Trang chủ (2026-09-25):** người dùng yêu cầu các thẻ/banner chỉ có tiêu đề + mô tả phải bấm được để xem nội dung sâu hơn (chữ, ảnh, album, video), dễ thêm nội dung về sau, không redesign. Đã làm: 6 thẻ "Album ảnh đẹp" thành link tới album concept tương ứng (`#/album/<dịch vụ>/<concept>`); 5 ô Concept, khung Video (nút ▶ trước đây bấm không làm gì), 3 bài Tin tức (trước đây ẩn "Đọc thêm" vì chưa có bài), "Tìm hiểu thêm" ở Giới thiệu/Chụp tại nhà/Quy trình mở trang nội dung `#/noi-dung/<slug>` (`js/content.js`, view `view-content` trong `index.html`). Mỗi trang có breadcrumb về đúng section, nút Về trang chủ/Đặt lịch, mục "Có thể ba mẹ quan tâm". Tìm kiếm trên nav tìm được các trang này. **Đổi quyết định cũ (người dùng chốt):** bấm Album/Concept trên Trang chủ giờ xem công khai, KHÔNG bắt đăng nhập nữa (đã bỏ gate trong `js/script.js`); Đặt lịch/Ảnh của tôi vẫn gate như cũ. Nội dung bài viết không đặt số liệu nghiệp vụ chưa chốt (mức cọc, giữ chỗ, dời lịch: ghi "theo chính sách hiện hành, Sale báo rõ"); ảnh/video minh hoạ đều có ghi chú không phải khách thật. Sửa kèm 1 lỗi có từ trước: 4/6 thẻ Album bị lệch ảnh so với tên (vd "Gia đình sum vầy" hiện ảnh 1 bé, "Sinh nhật rực rỡ" hiện mẹ bầu) - đã khớp lại; thẻ tin nổi bật có khoảng trắng lớn dưới mô tả trên desktop - ảnh giờ giãn lấp chỗ trống. Test: `_screenshots/test-content-pages.js` (59 case desktop+mobile, không cần server); `test-interest-gate.js` đã sửa 3 case theo quyết định mới.
- **Logo mới + chatbot tư vấn theo concept album (2026-09-25):** logo là **wordmark chữ** (người dùng chọn phương án 4/4 sau khi chê bản biểu tượng máy ảnh đầu tiên): "aloha" viết thường Quicksand + trái tim hồng nhỏ + dòng "BABY STUDIO" giãn chữ, là chữ HTML `.wordmark` (css/style.css, cỡ theo biến `--wm-size`) ở header/footer `index.html`, `crm/admin.html`, và thêm mới lên đầu thẻ `login.html`; link bọc ngoài có `aria-label` "ALOHA Baby" (tên thương hiệu không đổi). Biểu tượng `images/logo-mark.svg` = trái tim hồng, dùng cho favicon và ảnh đại diện khung chat; `images/logo.svg` = bản chữ để dùng ngoài web; favicon SVG + `images/favicon-32.png` + `images/apple-touch-icon.png` (xuất lại bằng `_screenshots/make-logo-png.js` khi sửa logo). Lưu ý đã gặp: comment XML trong SVG KHÔNG được chứa hai dấu gạch liền nhau (vd tên token CSS) nếu không cả file hỏng. Chatbot (`js/script.js`): menu chính thêm nhánh "Concept & ảnh mẫu" (chọn dịch vụ → 7 concept → ảnh bìa + mô tả + số ảnh → nút "Xem album concept này"/"Đặt lịch concept này"); nhánh tư vấn dịch vụ liệt kê đủ concept + nút xem ảnh mẫu; khung gõ tự do tự gắn nút mở album concept khi khách nhắc tên concept (`CONCEPT_KEYWORDS`, từ khoá dùng chung như "biển"/"vintage" chọn theo dịch vụ khách đang nhắc) và trả lời dự phòng theo concept khi AI lỗi. Dữ liệu concept đọc từ `window.AlohaAlbums.list` (cùng nguồn với trang album). Server (`server/server.js`): system prompt có đủ 35 concept, thêm action `album-<dịch vụ>` vào whitelist `ACTIONS` (đã thử với Gemini thật: gợi ý đúng nút album) — **cần push để Render deploy lại** thì bản public mới có. Test: `_screenshots/test-chat-concepts.js` (25 case, không cần server).
- **Hero Banner 5 dịch vụ + album riêng từng dịch vụ (2026-09-25):** section "5 dịch vụ đồng hành cùng con lớn khôn" lên đầu trang chủ ngay dưới navbar, thành Hero Banner chiếm gần trọn màn đầu (`.svc-hero`, giữ `id="dich-vu"` cho menu/tìm kiếm/chatbot): cột chữ (h1 + mô tả + nút đặt lịch; danh sách 01-05 dịch vụ bên dưới tiêu đề đã bỏ theo yêu cầu người dùng vì thừa, trùng với collage) + collage bất đối xứng 5 ảnh (Newborn ô lớn). Album 3 tầng (người dùng chốt): mỗi ô/dòng dịch vụ mở `#/album/<dịch vụ>` = danh sách concept album (tên concept khớp `SERVICE_INFO` của chatbot, 3 concept/dịch vụ), bấm concept mở `#/album/<dịch vụ>/<concept>` = toàn bộ ảnh (view `view-album` trong `index.html`, dữ liệu + render + lightbox ở `js/albums.js`): lưới masonry không giới hạn số ảnh, link "Tất cả concept" quay lên 1 tầng, lightbox (phím mũi tên/vuốt/Esc), chip chuyển album, nút "Về trang chủ" ở đầu và cuối trang, link album sai thì báo không tìm thấy. Album xem công khai KHÔNG bắt đăng nhập (khác Album/Concept ở dưới vốn gate theo yêu cầu cũ) — cân nhắc lại nếu người dùng muốn đồng bộ. Ảnh album hiện dùng lại ảnh stock sẵn có (đã bỏ 2 ảnh trùng byte `album-5`=`service-be-lon`, `album-6`=`concept-sinh-nhat`); sau đó tải thêm 120 ảnh stock Pexels cho 15 concept bằng `_screenshots/fetch-album-photos.js` (lưu `images/albums/<dịch vụ>/<concept>/`, nguồn từng ảnh ở `images/albums/sources.json`), xem lại từng ảnh bằng contact sheet và loại 8 ảnh (váy xuyên thấu, gần trùng, sai chủ đề gia đình) -> còn 112 ảnh mới, cả 15 concept đều hiện. Đợt 2 (người dùng yêu cầu "đa dạng concept"): thêm 4 concept/dịch vụ (Newborn: Hoa lá, Thú ngộ nghĩnh, Đen trắng, Trăng sao; Bầu: Cùng chồng, Cùng bé lớn, Vòng hoa, Biển; Sinh nhật: Trung thu, Công chúa, Tiệc gia đình, Picnic; Bé lớn: Áo dài, Nghề nghiệp, Mùa thu, Thể thao; Gia đình: Biển, Nhiều thế hệ, Anh chị em, Dã ngoại), tải 180 ảnh (w=1000), loại 36 (chủ yếu nhiều tấm cùng 1 buổi chụp, trùng concept cũ, không có em bé) -> mỗi dịch vụ 7 concept, tổng 35 concept, 256 ảnh trong `images/albums/` (~40MB); concept "Lễ hội" đổi tên "Giáng sinh (Noel)" vì ảnh toàn Noel. Router: chuyển view giờ nhảy lên đầu ngay (`behavior:"instant"`) thay vì trôi theo CSS smooth ~300ms. Hero cũ ("Lưu giữ từng khoảnh khắc...") chuyển xuống ngay dưới, nội dung giữ nguyên, `h1` đổi thành `h2.hero-title` (trang chỉ 1 h1). Đã xoá CSS `.services-showcase/.showcase-*` không còn dùng. **Sửa kèm 1 lỗi thật có từ trước:** trên mobile/tablet (≤1180px) danh sách con "Dịch vụ" của menu hamburger đặt cứng `visibility:visible; pointer-events:auto` nên khi menu ĐÓNG nó vẫn nằm vô hình đè lên trang và cướp cú chạm ở vùng y≈250-450px (chạm nút lại nhảy tới `#dich-vu`) — đổi thành `inherit`. Test: `_screenshots/test-service-albums.js` (130 case desktop+mobile, không cần server/đăng nhập); các test cũ vẫn pass.
- **Trang chủ mobile: giảm khoảng cách giữa các section + tăng cỡ tiêu đề mỗi phần:** người dùng chê (sau khi đã sửa lỗi ảnh Hero biến mất ở mục trên) khoảng cách giữa các section trên mobile vẫn hơi xa, tiêu đề `<h2>` mỗi phần chưa đủ to/nổi bật. Nguyên nhân: mọi section (Dịch vụ/Concept/Giới thiệu/Album/Tin tức/Quy trình/Video) đều dùng chung `padding: 96px 0` y hệt desktop, không có override cho mobile/tablet dù đã thu về 1 cột hẹp hơn hẳn; tiêu đề `.section-head h2` dùng `clamp(28px, 4vw, 38px)` nhưng ở mọi màn ≤700px thì `4vw` luôn nhỏ hơn 28px nên LUÔN kẹt ở đúng mức sàn 28px, không lớn hơn được; 2 tiêu đề "Giới thiệu"/"Video giới thiệu" (`.about-content h2`, `.video-text h2`) còn không nằm trong `.section-head` nên rơi về cỡ h2 mặc định của trình duyệt, còn nhỏ hơn nữa và không đồng nhất với các section khác. Đã sửa: (1) đổi clamp tiêu đề thành `clamp(32px, 8vw, 38px)` (đo thực tế trên trang: 32px ở mọi mốc điện thoại, giữ nguyên 38px desktop) + thêm rule riêng cho `.about-content h2`/`.video-text h2` dùng cùng clamp cho đồng nhất; (2) thêm override padding section theo 2 nấc - `≤1120px` (tablet+mobile): 96px→64px, `≤560px` (điện thoại): tiếp tục giảm còn 48px (`.hero` và `.process`/`.video` giảm tương ứng theo tỉ lệ vì cấu trúc padding khác 1 chút: chỉ có top hoặc chỉ có bottom); `.section-head` margin-bottom cũng giảm theo (48px gốc → 36px → 28px) để tiêu đề không trôi xa nội dung ngay bên dưới nó. Đã xác minh: không tràn ngang, desktop (>1120px) không đổi gì, chạy lại test hồi quy vẫn pass.
- **Sửa lỗi thật: ảnh Hero biến mất trên mobile, để lại khoảng trống lớn trước "5 dịch vụ...":** người dùng báo trang chủ mobile có khoảng trống to trước heading "5 dịch vụ đồng hành cùng con lớn khôn". Kiểm tra bằng Puppeteer (đo `getBoundingClientRect` thực tế, không đoán qua đọc CSS) phát hiện `.hero-visual` (khối chứa ảnh bé ở Hero) bị co về đúng `0x0` trên mọi màn ≤1120px — ảnh không hề bị lỗi tải (load OK, `naturalWidth:1200`), mà do lỗi CSS: ở breakpoint mobile/tablet, `.hero-visual` chỉ có `max-width:360px; margin:40px auto 0;` (không có `width` tường minh) trong khi nó là 1 grid item cao phụ thuộc `aspect-ratio` (qua `.hero-card`) - tổ hợp "margin:auto + max-width nhưng không có width" trên 1 grid item kiểu này khiến Chrome tính sai row cao ngầm định của grid thành đúng bằng phần margin-top (40px), bỏ qua hẳn nội dung ảnh. Sửa: thêm `width:100%` tường minh cạnh `max-width:360px` để layout có kích thước xác định ngay, không rơi vào đường tính toán mơ hồ đó nữa. Đã xác minh lại: ảnh Hero hiện đúng trên 480/768/900px, khối cao đúng 450px (360×5/4); desktop (1440px, không qua breakpoint này) không đổi. Lỗi có từ trước, không phải do các thay đổi khác trong session này.
- **Dashboard Sếp màu mè/trực quan hơn (theo research xu hướng dashboard 2026) + 2 lỗi thật phát hiện khi làm:** người dùng chê giao diện Sếp "chưa ok", nhờ research xu hướng UI mới nhất rồi làm "wow hơn màu mè hơn". Đã search web (xem nguồn cuối mục này) xác nhận xu hướng dashboard 2026: KPI card có icon + sparkline xu hướng, màu accent dùng có chủ đích thay vì trắng đơn sắc, funnel/biểu đồ nên phân biệt màu theo bậc. Áp dụng đúng các ý này NHƯNG tái dùng đúng bảng màu badge trạng thái đã có sẵn (không tạo bảng màu mới, đúng guardrail `rules/design.md`): (1) 5 thẻ KPI (`crm/admin.html`) giờ mỗi thẻ có icon riêng (người/chat/lịch/máy ảnh/dấu tích) + sparkline SVG nhỏ minh hoạ xu hướng + viền màu trên cùng, màu khớp đúng bậc phễu (tím/cam/hồng/xanh dương/xanh lá); (2) phễu khách hàng (`crm/js/admin.js` `FUNNEL_DATA`) mỗi thanh 1 màu riêng cùng bộ màu trên thay vì 5 thanh hồng đơn sắc; (3) biểu đồ "Khách hàng mới theo tuần" thêm nhãn số liệu phía trên mỗi cột. Nhân tiện phát hiện + sửa 2 lỗi có sẵn từ trước (không phải do các thay đổi trước đó trong session): (a) 3 nút tab "Sắp tới/Hôm nay/Đã qua" ở mục Lịch hẹn hiện như nút mặc định trình duyệt (viền đen vuông) vì dùng class `.ps-tab` nhưng `crm/admin.html` không nạp `css/pages.css` (nơi định nghĩa class đó) - đã định nghĩa lại `.ps-tabs`/`.ps-tab` riêng trong `css/admin.css` (không import cả file pages.css để tránh lẫn style khác phạm vi); (b) nav tab admin trên mobile (≤720px, dùng `overflow-x:auto` để cuộn ngang) bị vỡ nặng - các thẻ `<a>` không có `white-space:nowrap`/`flex-shrink:0` nên bị bóp hẹp, chữ "Ảnh & chỉnh sửa" ngắt dòng nhiều dòng kéo TẤT CẢ tab khác (kể cả "Dashboard") giãn cao theo, cộng `border-radius` dạng pill biến tab "Dashboard" thành 1 khối tròn hồng to bất thường thay vì dải pill cuộn ngang gọn - đã thêm `white-space:nowrap; flex-shrink:0` cho `.admin-tabs a` để tab giữ đúng kích thước 1 dòng và cuộn ngang đúng như thiết kế. Đã chạy lại test hồi quy, vẫn pass; kiểm tra trực quan cả desktop/mobile bằng screenshot.
  - Nguồn tham khảo research: [Admin Dashboard Design Examples for 2026](https://www.fanruan.com/en/blog/top-admin-dashboard-design-ideas-inspiration), [Bento Grid Dashboard Design: Complete Guide 2026](https://www.orbix.studio/blogs/bento-grid-dashboard-design-aesthetics), [Dashboard UI design: From KPIs to layouts that convert](https://www.setproduct.com/blog/dashboard-ui-design), [Best Dashboard Design Patterns 2026](https://artofstyleframe.com/blog/dashboard-design-patterns-web-apps/).
- **Thông báo 2 chiều Khách ↔ Thợ ảnh + cập nhật gần-real-time trong cùng trình duyệt:** người dùng yêu cầu (1) khách nhận thông báo khi ảnh sửa xong, (2) Thợ ảnh nhận thông báo khi khách có yêu cầu mới, (3) "4 phần Sale/Khách/Sếp/Thợ luôn đồng bộ dữ liệu real-time". Đã làm được (1) và (2) trong giới hạn kiến trúc site tĩnh hiện tại: thêm 2 field `staffSeen`/`customerSeenDone` vào mỗi edit request (`js/data-store.js`) + 2 hàm `AlohaData.markStaffSeen()`/`markCustomerSeenDone()`. Phía khách (`js/script.js` `renderNotifications()`): trạng thái "Hoàn thành" giờ hiện nổi bật riêng ("Ảnh đã sửa xong · <mã đơn>", nền xanh) thay vì bị lẫn với các dòng đang xử lý như trước (bug cũ: chấm đỏ từng TẮT ngay khi ảnh xong vì logic chỉ coi "chưa Hoàn thành" là có gì đó đáng báo — ngược hẳn với ý "báo khi xong"), chỉ tắt highlight/chấm đỏ khi khách THẬT SỰ mở panel xem (không tự ý mark-seen lúc polling nền, tránh mất thông báo mà khách chưa kịp thấy). Phía Thợ ảnh (`crm/js/admin.js`): thêm badge số đếm yêu cầu thật chưa mở xem ngay trên tab "Ảnh & chỉnh sửa" (`updateTabBadge()`), tắt khi mở modal xem yêu cầu đó. Cả 2 phía đều có `setInterval` polling 5 giây đọc lại `aloha_demo_db` để tự cập nhật badge/danh sách khi dữ liệu đổi ở tab/khung khác **cùng trình duyệt**, không cần tải lại trang — đây là mức "real-time" tối đa làm được khi chưa có backend, đúng pattern "mô phỏng real-time" đã dùng cho `js/dat-lich.js` trước đó.
  - **Chưa làm được / không làm được trong yêu cầu (3), đã nói rõ với người dùng, không âm thầm bỏ qua:** (a) đồng bộ dữ liệu thật giữa Sale/Khách/Sếp/Thợ trên **các thiết bị/trình duyệt khác nhau** là KHÔNG THỂ với kiến trúc localStorage hiện tại (mỗi trình duyệt có kho dữ liệu riêng, không có server/database/kênh đẩy dữ liệu thật) — cần một backend thật (Node/Express + database + WebSocket hoặc SSE) mới làm được, đây là quyết định kiến trúc lớn chưa được chốt (xem `rules/tech-defaults.md` mục Kiến trúc: "không tự chọn framework/database thay người dùng"), không tự ý triển khai. (b) Riêng phần **Sale** hiện còn chưa kết nối được vào vòng đồng bộ này dù chỉ trong cùng trình duyệt: bảng "Khách hàng/CRM" và "Lịch hẹn" Sale/Sếp đang xem (`CUSTOMERS`/`APPOINTMENTS` trong `crm/js/admin.js`) vẫn là mảng dữ liệu tĩnh viết cứng, hoàn toàn tách biệt khỏi `aloha_demo_db` thật (khác với "Quản lý chụp & chỉnh ảnh" đã nối AlohaData thật) — đây là khoảng trống có từ trước, không phải lỗi phát sinh từ thay đổi lần này, nhưng khiến Sale chưa thể "đồng bộ real-time" theo đúng nghĩa với 3 vai trò còn lại cho tới khi có việc nối CRM/Lịch hẹn của Sale vào một nguồn dữ liệu thật dùng chung.
  - Đã kiểm tra bằng Puppeteer: badge Thợ ảnh xuất hiện qua polling khi khách gửi yêu cầu từ tab khác (không tải lại trang), tắt khi mở xem; khách thấy đúng "Ảnh đã sửa xong" nổi bật lần đầu mở panel sau khi Thợ ảnh hoàn thành, không còn nổi bật ở lần mở thứ 2, chấm đỏ tắt đúng lúc. Chạy lại 45 test hồi quy hiện có, vẫn pass.
- **Bỏ bước "Chờ QC" riêng khỏi quy trình chỉnh sửa ảnh:** người dùng hỏi "Chờ QC là gì" và xác nhận trực tiếp quy trình thật của Thợ ảnh chỉ có 3 bước: xác nhận yêu cầu (Chờ xử lý) → đang sửa ảnh (Đang thực hiện) → tải ảnh đã sửa lên là hoàn thành, KHÔNG qua một vai trò QC riêng duyệt trước khi hoàn thành — mâu thuẫn với tài liệu cũ (`rules/workflow.md`/`rules/tech-defaults.md` từng mô tả "Chờ QC → QC kiểm tra → không đạt thì trả lại retoucher" như 1 bước/vai trò tách riêng). Đã xác nhận đây thật ra là điểm KHÔNG nhất quán giữa 2 tài liệu cũ: `rules/operations.md` (khâu 7 "Hậu kỳ — Retoucher + QC") vốn đã mô tả retouch+QC là MỘT khâu do cùng người làm, chỉ `workflow.md`/`tech-defaults.md` từng tách thành trạng thái riêng — nay sửa lại cho khớp đúng thực tế người dùng xác nhận. Đã đổi: `STATUS_FLOW` trong `js/data-store.js` còn 3 giá trị (`Chờ xử lý, Đang thực hiện, Hoàn thành`); bỏ hẳn cột "Chờ QC" khỏi kanban (`crm/admin.html`, `.kanban` chuyển `repeat(4,1fr)` → `repeat(3,1fr)`) và mọi map liên quan trong `crm/js/admin.js` (`STATUS_COL_ID`, `STATUS_COUNT_ID`, `STATUS_ACCENT`, `STATUS_BADGE_CLASS`); thẻ minh hoạ tĩnh `static-3` (trước ở "Chờ QC", đã có đủ ảnh đánh dấu xong) chuyển thẳng sang "Hoàn thành". Nút "Chuyển sang bước tiếp theo" ở "Đang thực hiện" vẫn giữ nguyên gate (khoá tới khi Thợ ảnh tick xong hết ảnh) — giờ đây transition đó đi thẳng tới "Hoàn thành" luôn, đúng nghĩa "tải ảnh đã sửa lên là hoàn thành". Đã cập nhật `rules/tech-defaults.md` (Status System) và `rules/workflow.md` (mục "Chọn & chỉnh sửa ảnh") cho khớp. Chạy lại 45 test tự động, tất cả pass; không còn chỗ nào trong code/UI nhắc tới "Chờ QC".
- **Kanban "Quản lý chụp & chỉnh ảnh" thêm màu nhận diện riêng cho từng giai đoạn:** người dùng chê 4 cột (Chờ xử lý/Đang thực hiện/Chờ QC/Hoàn thành) nhìn giống hệt nhau, muốn trực quan hơn/màu mè hơn. Đã thêm mỗi cột 1 màu nhận diện riêng (tái dùng ĐÚNG bảng màu badge trạng thái đã có sẵn trong `css/admin.css` — không tạo bảng màu mới, tránh phá `rules/design.md`): Chờ xử lý = tím indigo, Đang thực hiện = cam, Chờ QC = xanh dương, Hoàn thành = xanh lá. Áp dụng màu này lên: viền trên + nền nhạt của cột, icon + chữ tiêu đề cột (icon SVG line-style tự vẽ, cùng phong cách icon có sẵn của site, không dùng thư viện ngoài), viền trái mỗi thẻ, số đếm yêu cầu trong cột (badge tròn góc phải header), thanh tiến độ trong thẻ VÀ trong modal chi tiết (dùng CSS custom property `--col-accent` cascade từ cột xuống thẻ; modal nằm ngoài cột nên set `--col-accent` bằng inline style qua map `STATUS_ACCENT` trong `admin.js`, khớp đúng màu badge trạng thái trong modal). Thêm mũi tên nối nhỏ giữa các cột (giống pattern "đường nối 4 bước" đã dùng ở section Quy trình trang chủ) để nhấn mạnh luồng tuần tự, tự ẩn ở màn hình 2 cột (≤1120px) vì hướng mũi tên không còn đúng sau khi xuống dòng. Thêm trạng thái rỗng "Chưa có yêu cầu nào ở bước này" khi 1 cột không có thẻ nào (trước đây cột rỗng nhìn như bị lỗi/thiếu style). Đã kiểm tra lại toàn bộ 45 test tự động vẫn pass (vài lần chạy dồn nhiều file Puppeteer liên tiếp bị flaky do tranh chấp tài nguyên trình duyệt, chạy riêng từng file thì pass 100% - không phải lỗi do thay đổi lần này, đã xác minh bằng cách chạy lại riêng lẻ).
- **Kanban "Quản lý chụp & chỉnh ảnh" giờ bấm được, có ưu tiên xử lý + xem đúng ảnh + Thợ ảnh tự confirm tiến độ:** người dùng chỉ ra thẻ kanban (Chờ xử lý/Đang thực hiện/...) không bấm được, và với vai trò Thợ ảnh cần biết yêu cầu nào tới trước, yêu cầu gồm ảnh nào, và cách tự confirm tiến độ để Sếp quan sát. Đã làm: (1) toàn bộ thẻ (kể cả 5 thẻ minh hoạ tĩnh trước đây hard-code trong HTML, không ảnh không bấm được) chuyển sang render động từ `STATIC_REQUESTS` (mảng demo trong `crm/js/admin.js`, giống cách `CUSTOMERS`/`APPOINTMENTS` đã làm — không lưu localStorage) gộp cùng yêu cầu thật từ `AlohaData.getEditRequests()`, dùng chung 1 pipeline hiển thị/bấm/modal; (2) mỗi thẻ hiện số ưu tiên (`#1`, `#2`...) xếp theo `createdAt` tăng dần trong từng cột (đến trước xếp trước) kèm "Gửi X giờ/ngày trước"; (3) bấm vào thẻ mở modal chi tiết hiện đúng danh sách ảnh của yêu cầu đó (field mới `photos`, xem schema `aloha_demo_db` ở trên) kèm ghi chú riêng từng ảnh nếu có; (4) Thợ ảnh (role `tho-anh`) có nút "Đánh dấu đã xong" trên từng ảnh trong modal (ghi vào `doneIds`, persist qua `AlohaData.togglePhotoDone` cho yêu cầu thật, tạm trong bộ nhớ JS cho thẻ minh hoạ tĩnh), thanh tiến độ "Đã xong X/Y ảnh" cập nhật ngay; Sếp xem cùng dữ liệu này ở chế độ chỉ đọc (không có nút đánh dấu) — đúng yêu cầu "Thợ ảnh confirm, Sếp quan sát"; (5) nút "Chuyển sang bước tiếp theo" khi đang ở "Đang thực hiện" bị khoá (disabled + title giải thích) cho tới khi đã đánh dấu xong hết ảnh, tránh chuyển sang Chờ QC khi việc chưa xong thật — không áp dụng gate này cho các bước khác (Chờ xử lý→Đang thực hiện, Chờ QC→Hoàn thành) vì không phải bước Thợ ảnh trực tiếp làm xong ảnh. Yêu cầu cũ đã lưu trước khi có field `photos`/`doneIds` vẫn hiển thị bình thường (fallback: không có ảnh chi tiết thì không hiện gate, không crash). Đã cập nhật 2 test cũ (`test-photos-chat.js`, `test-photo-notes.js`) cho khớp cấu trúc DOM mới (bỏ class `.dynamic-request` không còn tồn tại) và khôi phục đúng gợi ý "+N ảnh có ghi chú riêng" trên thẻ (bị rơi mất khi viết lại card, phát hiện qua chính bộ test cũ) — toàn bộ 45 case (26 cũ + 19 mới) đang pass.
- **Chữ đè lên ảnh dễ đọc hơn + icon Tìm kiếm/Thông báo giờ dùng thật được:** (1) người dùng chê caption trên ảnh (Dịch vụ/Concept/Album) khó đọc — phát hiện lỗi thật: 3/5 caption ở Concept ("Noel", "Sinh nhật", "Hàn Quốc") bị gán nhầm `color: navy-900` (CSS cũ từ thời chưa có ảnh thật, quên xoá) khiến chữ tối đè lên lớp phủ tối, gần như không đọc được — đã xoá; đồng thời thêm token `--caption-shadow` (bóng đổ kép) cho mọi caption đè-lên-ảnh (Concept/Album/Services mosaic) để chữ luôn rõ kể cả trên ảnh sáng màu (vd tile "Vintage"), và **giảm** lớp phủ tối (opacity/vùng phủ) ở cả 4 chỗ vì giờ không cần dựa hết vào việc làm tối ảnh mới đọc được chữ — đúng yêu cầu "dễ nhìn mà không che hết ảnh". (2) 2 icon Tìm kiếm/Thông báo trên nav trước đây bấm không làm gì — đã làm thật: **Tìm kiếm** mở panel gõ-để-lọc trong danh mục 5 dịch vụ + 5 concept + section trang chủ + Đặt lịch/Ảnh của tôi (không dấu vẫn tìm được), chọn kết quả tự quay về Trang chủ (nếu đang ở view khác) rồi cuộn tới đúng chỗ; **Thông báo** đọc dữ liệu THẬT từ `AlohaData.getEditRequests()` lọc theo `phone` của khách đang đăng nhập, hiện đúng trạng thái từng yêu cầu chỉnh sửa ảnh, chấm đỏ chỉ hiện khi có yêu cầu chưa "Hoàn thành"; chưa đăng nhập thì mời đăng nhập thay vì hiện rỗng. Khi thêm 2 icon này phải nới lại toàn bộ breakpoint mobile: nav login đã đăng nhập ("Ảnh của tôi" + icon Đăng xuất) dài hơn hẳn lúc chưa đăng nhập nên bị tràn ở đúng 320-360px dù các mốc 375px+ vẫn đủ chỗ — xử lý bằng breakpoint riêng (`max-width:360px`) chỉ ẩn Thông báo ở dải hẹp nhất đó, còn lại (kể cả hầu hết điện thoại thật 375-430px) vẫn thấy được, thay vì ẩn cả 2 icon ở ngưỡng 480px như bản vá tạm trước đây (sẽ ẩn mất trên gần như mọi điện thoại thật, phản tác dụng vì giờ đây là tính năng thật).
- **Rà soát tổng thể flow/bố cục/design sau các lượt sửa trên** (người dùng hỏi "đã hợp lý chưa"): chụp + xem trực tiếp toàn luồng (Trang chủ, Đăng nhập, 3 bước Đặt lịch, Ảnh của tôi, Dashboard Sếp đầy đủ). Phát hiện 2 việc: (1) **lỗi thật** — mục "Trang chủ" trên nav vẫn sáng/active dù đang ở view `#/dat-lich`/`#/chon-anh` (router chưa cập nhật trạng thái active khi đổi view) → đã sửa `js/router.js` (`showView()` giờ tắt active trên `#navHomeLink` khi rời Trang chủ). (2) **nợ kỹ thuật nhỏ** — hiệu ứng fade-in scroll trong `crm/js/admin.js` tự viết riêng (inline style, IntersectionObserver), không tôn trọng `prefers-reduced-motion` như `.reveal` bên khách hàng → đã sửa để bỏ qua animation khi người dùng bật giảm hiệu ứng. Ngoài 2 điểm này, review không thấy vấn đề bố cục/design mới; các phần đã sửa trước đó (nav wrap, Album mosaic, Album/Concept click-through, link chết) đều đúng như kỳ vọng khi xem lại. Lưu ý cho lần sau: 2 lần review trước đó từng "tưởng thiếu nội dung" (concept-tile thứ 5, các section Dashboard Sếp sau CRM) hoá ra chỉ là chụp ảnh quá sớm giữa lúc animation cuộn đang chạy — nhớ cuộn hết trang (hoặc tắt animation qua `prefers-reduced-motion`, riêng CRM Sếp phải cuộn thật vì animation không nghe media query này) trước khi đánh giá bố cục qua screenshot.
- **Sửa section "Album ảnh đẹp" bị "trống"/phẳng:** người dùng chê trang chủ trống và ngắn gọn quá — audit bằng screenshot (tắt animation qua `prefers-reduced-motion` để nhìn đúng trạng thái cuối, tránh nhầm với timing của hiệu ứng cuộn) xác nhận section Album là chỗ thưa thớt rõ nhất: 1 hàng 6 ô vuông đều nhau, phẳng hẳn so với Dịch vụ/Concept liền kề (đều đã có bố cục ảnh lớn + mosaic). Đã đổi `.album-grid` sang bento 2 ô lớn (2x2) kẹp 4 ô nhỏ ở giữa (desktop 6 cột, tablet 3 cột, mobile xếp dọc 1 ô lớn + 5 dải ngang) — dùng lại đúng 6 ảnh cũ, không cần ảnh mới. (Đã xác minh: 5/5 concept-tile vẫn hiển thị đủ như thiết kế, không phải lỗi — lần đầu tưởng thiếu do chụp screenshot quá sớm giữa lúc hiệu ứng stagger-fade còn đang chạy.)
- **Sửa nav bị vỡ dòng chữ + các mục "bấm không ra gì" trên trang chủ:** (1) nav-links (7 mục: Trang chủ...Album ảnh đẹp) bị ngắt dòng ở MỌI kích thước desktop (vd "Trang chủ" tách "Trang"/"chủ") — nguyên nhân: `.container` giới hạn tối đa 1180px nên nav luôn bị chật dù màn hình rộng bao nhiêu, đo bằng Puppeteer (`getClientRects().length`) xác nhận cả 7 mục đều vỡ dòng. Đã tách riêng breakpoint chuyển sang hamburger cho nav từ 1120px lên 1180px (không đụng breakpoint layout khác vốn dùng chung khối 1120px cũ) + thêm `white-space:nowrap` + giảm gap/kích thước icon một chút để nav luôn đủ chỗ hiện 1 hàng. (2) Album/Concept trên trang chủ: khi khách ĐÃ đăng nhập, bấm vào **không làm gì cả** (gate logic cũ chỉ xử lý nhánh chưa đăng nhập, quên nhánh đã đăng nhập) — đã hoàn thiện: đã đăng nhập bấm Album → `#/chon-anh` thật, bấm Concept → `#/dat-lich` thật. (3) 4 link chết topbar/menu ("Trở thành đối tác", "Hỗ trợ", "Báo giá", "Khuyến mại") nay mở khung chat tư vấn thật thay vì `href="#"` không làm gì. (4) 3 nhóm link chết còn lại (social icon footer, cột "Chính sách" footer, "Đọc thêm" ở 2/3 thẻ Tin tức) — đã hỏi người dùng, chọn phương án "chỉ ẩn tạm, chưa viết nội dung": đã bỏ khỏi DOM (còn lại HTML comment ghi rõ lý do + điều kiện thêm lại), không để lộ link chết trên giao diện. Riêng thẻ Tin tức "Cách đặt lịch và đặt cọc..." đổi "Đọc thêm" thành "Đặt lịch ngay →" trỏ thẳng `#/dat-lich` (dùng lại tính năng thật đã có, không phải nội dung mới). `footer-grid` đổi từ 5 cột sang 4 cột (`grid-template-columns`) cho khớp số cột còn lại, tránh khoảng trống thừa bên phải.
- **Chatbot tư vấn sâu hơn + có ảnh minh hoạ, polish UI/UX nhẹ:** nhánh kịch bản "Tư vấn dịch vụ & báo giá" (`js/script.js`, `SERVICE_INFO`) giờ mỗi dịch vụ có 3 gợi ý concept (thay vì 1), 1 câu lưu ý thực tế, ghi chú gói ảnh tham khảo (dùng lại đúng số 15 ảnh/gói đã có ở `js/chon-anh.js` để nhất quán) và **1 ảnh thật minh hoạ** (`images/service-*.jpg`, đúng ảnh dịch vụ đã có sẵn) gửi kèm qua hàm mới `addImageMsg()` (bubble `.chat-media`, ảnh + chú thích). "Quy trình đặt lịch" giải thích đủ cả hậu kỳ/chọn ảnh, không dừng ở bước đặt cọc. FAQ tăng từ 3 lên 6 câu (thêm chụp tại nhà, chưa biết chọn concept, ảnh gốc/ảnh chỉnh sửa khác gì, đổi lịch hẹn). `server/server.js` (system prompt Claude API cho khung nhập tự do) cập nhật tương ứng để trả lời sâu/nhất quán như kịch bản, vẫn giữ nguyên guardrail không bịa số liệu cọc/chính sách. Đồng thời rà soát UI/UX toàn site: thêm trạng thái `:focus-visible` (viền hồng) dùng chung cho a/button/input/textarea/select (trước đó chưa có, chỉ có viền xanh mặc định của trình duyệt, lệch tông); thêm hover lift cho `.concept-tile` (trước đó là item hover-thiếu duy nhất trong nhóm thẻ ảnh-gradient, các thẻ khác như `.showcase-item`/`.album-card`/`.news-card` đã có sẵn). Không đổi bảng màu/layout gốc (đúng guardrail `rules/design.md`), không đổi nội dung/chức năng nào khác. 41+ test Puppeteer hiện có vẫn pass.
- **Audit responsive mobile + sửa 2 lỗi thật:** người dùng yêu cầu giao diện "ổn ở desktop, ưu tiên chủ yếu trên di động" → chạy audit tự động đo `scrollWidth` thực tế ở 320/375/390/768/1440px (`_screenshots/audit-responsive.js`, script giữ lại để tái dùng khi cần) trên mọi màn hình thật kể cả các view cần đăng nhập. Phát hiện và sửa: (1) nav header tràn ngang thật ở màn ≤360px (iPhone SE/5) khiến nút hamburger bị đẩy ra ngoài viewport, không mở được menu — thêm breakpoint `max-width:480px` trong `css/style.css` ẩn 2 icon tĩnh (tìm kiếm/thông báo, không đụng icon Đăng xuất) + thu gọn nút đăng nhập; (2) nút nổi "Đặt lịch" + chat che một phần nội dung tương tác (thẻ dịch vụ, nút thả tim ảnh) ở view `#/dat-lich`/`#/chon-anh` trên mobile ngay từ lần cuộn đầu — `js/router.js` gắn class `hide-floating-book` lên `<body>` khi không ở Trang chủ, ẩn hẳn nút "Đặt lịch" nổi (thừa vì khách đã ở đúng luồng), chỉ giữ nút chat. Đã kiểm tra lại 41 test Puppeteer hiện có vẫn pass, không có lỗi tràn ngang nào còn sót ở mọi kích thước đã đo.
- **Khách hàng gộp còn 1 file `index.html` (SPA nhỏ dùng hash, `js/router.js` mới):** người dùng nhận ra `index.html`/`dat-lich.html`/`chon-anh.html` lặp lại ~40-50% khung (topbar/nav/footer/chat) do site tĩnh không có template → đã gộp 3 file này thành 1, xoá `dat-lich.html` và `chon-anh.html`. Route hash: `#/` (trang chủ), `#/dat-lich`, `#/chon-anh`; neo cuộn trong trang chủ (`#dich-vu`, `#gioi-thieu`...) không có dấu `/` nên không bị hiểu nhầm là route. Toàn bộ dự án giờ chỉ còn **3 file HTML**: `index.html`, `login.html`, `crm/admin.html`. Chi tiết cơ chế: `rules/design.md` mục "Trang khách hàng: 1 file index.html dạng SPA nhỏ".
- **Trang chủ**: cải tiến UI/UX theo Taste Skill (anti-slop), 23 ảnh thật thay gradient/icon ở toàn bộ khu vực bán hàng chính (Hero, Dịch vụ, Album, Concept, Giới thiệu, Video, Tin tức) — ảnh stock miễn phí bản quyền (Pexels License), lưu tại `images/`, đã audit từng ảnh bằng mắt và sửa lại các ảnh bị gán sai chủ đề do lỗi scraping ban đầu (script lấy nhầm khối "Discover/Featured" cố định thay vì kết quả tìm kiếm thật — đã sửa selector).
- **Đăng nhập/phân quyền demo** (mô phỏng phía client, site tĩnh chưa có backend): `login.html` (form số điện thoại + mật khẩu, có chế độ Đăng ký, tham khảo bố cục kinhlac.online) + `js/auth.js` (module `window.AlohaAuth` dùng chung: `login/logout/requireRole/roleHome/getSession`).
- **2 giao diện thật sự theo vai trò:** Khách hàng (`index.html`, gate qua router — xem trên) và Ban quản trị (`crm/admin.html` — **1 file duy nhất** dùng chung cho Sale/CSKH, Thợ ảnh, Sếp, nội dung/tab tự ẩn hiện theo quyền qua `data-roles`, xem `rules/design.md` mục "Đăng nhập & phân quyền").
- **Gate hành vi thể hiện quan tâm** trên trang chủ: click Album/Concept/nút chat nổi/"Tư vấn concept ngay" khi chưa đăng nhập đều chuyển sang `login.html?next=...` (quay lại đúng trang sau khi đăng nhập/đăng ký), thay vì thực hiện hành động tại chỗ.
- **Nút Đăng nhập/Đăng ký riêng** trên nav trang chủ (`#navAuthArea`), tự đổi thành "Ảnh của tôi/Đặt lịch/Đăng xuất" khi đã đăng nhập khách hàng.
- Test Puppeteer (không phải test tự động CI, chạy tay khi cần): `_screenshots/test-auth.js` (17 case: phân quyền 4 vai trò, redirect, booking flow không vỡ sau khi gate) + `_screenshots/test-interest-gate.js` (10 case: gate quan tâm, đăng ký demo, next param) — toàn bộ đang pass.
- **Chatbot AI thật:** đã thêm `server/` — server Node/Express nhỏ (`npm install && npm start`, mặc định `http://localhost:3001`) proxy gọi **Claude API** (Anthropic, model `claude-haiku-4-5-20251001`), giữ `ANTHROPIC_API_KEY` trong `server/.env` (copy từ `.env.example`, không commit). Khung nhập tự do trong chat panel (`#chatInputForm`) gọi `POST /api/chat`; bước chọn dịch vụ/concept ban đầu vẫn là quick-reply kịch bản. Server chưa chạy hoặc thiếu key → chat báo lỗi thân thiện, không giả vờ có AI. Server này **chỉ phục vụ chat**, không phải backend CRM/booking — phạm vi đó vẫn chưa chốt.
- **Kịch bản chatbot đa dạng hơn + nút liên hệ Sale:** menu chính có 4 nhánh (Tư vấn dịch vụ & báo giá / Quy trình đặt lịch / Câu hỏi thường gặp / Liên hệ Sale ngay) thay vì đi thẳng vào chọn dịch vụ; mọi câu trả lời đều có lối quay lại menu hoặc gọi Sale. Thanh `.chat-sale-bar` (link `tel:0938125222`) hiện cố định trong khung chat dùng chung của `index.html`, không chỉ là 1 lựa chọn ẩn trong hội thoại.
- **"Ảnh của tôi" phân biệt đã/chưa chụp:** thêm `js/data-store.js` (`window.AlohaData`, localStorage key `aloha_demo_db`) làm kho dữ liệu demo dùng chung giữa Khách hàng và Ban quản trị (cùng trình duyệt). Tài khoản demo `0900000001` được seed sẵn `hasShoot:true`; khách đăng ký mới không có bản ghi → view `#/chon-anh` hiện trạng thái rỗng "Bạn chưa có ảnh nào" + CTA Đặt lịch, ẩn toàn bộ phần liên quan tới đơn/gói/lưới ảnh (kể cả phần đầu trang, không chỉ khối nội dung chính — đã có 1 lỗi hiển thị sót phần đầu trang, đã sửa). `session` trong `js/auth.js` nay có thêm field `phone` để định danh khách hàng cho việc tra cứu này.
- **Ảnh thật cho "Ảnh của tôi" + ghi chú riêng từng ảnh:** 16 ảnh newborn thật (Pexels License, `images/my-photos/photo-1.jpg`...`photo-16.jpg`) thay cho icon/gradient placeholder cũ (`ps-photo-ph` — đã xoá). Mỗi ảnh khi được chọn (thả tim) sẽ hiện thêm 1 dòng ghi chú riêng trong khối "Ghi chú riêng cho từng ảnh đã chọn" (thumbnail + textarea, cập nhật động theo lựa chọn), tách biệt với "Ghi chú chỉnh sửa chung cho cả bộ" đã có sẵn — cả hai loại ghi chú đều được gửi trong request (`AlohaData.createEditRequest` có thêm field `photoNotes`). Thợ ảnh thấy gợi ý "+N ảnh có ghi chú riêng" trên thẻ kanban động.
- **Request chỉnh sửa ảnh chuyển thật sang Thợ ảnh:** khi khách (có `hasShoot`) chọn ảnh và bấm "Gửi yêu cầu chỉnh sửa" trên view `#/chon-anh`, `AlohaData.createEditRequest()` lưu 1 bản ghi thật vào `aloha_demo_db.editRequests`. `crm/admin.html` (mục "Quản lý chụp & chỉnh ảnh") đọc danh sách này và chèn thẻ động (badge "Mới") vào đúng cột kanban theo `status`, cạnh các thẻ tĩnh minh hoạ — Thợ ảnh/Sếp đăng nhập trên **cùng trình duyệt** sẽ thấy ngay. Có nút "Chuyển sang bước tiếp theo" trên thẻ động để đẩy trạng thái theo đúng enum (Chờ xử lý → Đang thực hiện → Chờ QC → Hoàn thành). **Giới hạn cố hữu:** đây vẫn là mô phỏng bằng localStorage, chỉ hoạt động khi Khách hàng và Thợ ảnh dùng chung một trình duyệt/máy (đăng xuất rồi đăng nhập lại vai trò khác) — không đồng bộ qua nhiều thiết bị, cần backend thật mới làm được điều đó.
- Test Puppeteer bổ sung: `_screenshots/test-ai-chat.js` (server AI tắt → báo lỗi thân thiện) + `_screenshots/test-photos-chat.js` (9 case: trạng thái rỗng/có ảnh, request chuyển sang Thợ ảnh, đổi trạng thái, menu chatbot đa nhánh, thanh liên hệ Sale) + `_screenshots/test-photo-notes.js` (5 case: ảnh thật hiển thị, ghi chú riêng từng ảnh + ghi chú chung đều được lưu đúng, Thợ ảnh thấy gợi ý số ảnh có ghi chú) — toàn bộ đang pass, tổng cộng 41 case tự động trên 5 file test.

## Quyết định kỹ thuật (bản đầy đủ, kèm bối cảnh)

- Đăng nhập chỉ 1 trang, KHÔNG cho chọn vai trò bằng tay — hệ thống tự nhận diện qua 4 tài khoản demo hardcode trong `login.html` (hiển thị công khai dưới form để tự test), vì site tĩnh chưa có backend xác thực thật.
- 3 vai trò nội bộ gộp chung 1 file `crm/admin.html` thay vì 3 trang riêng (bản đầu đã làm 3 trang rồi bị yêu cầu gộp lại) — phân quyền bằng `data-roles` trên section/tab, JS **xoá khỏi DOM** phần không thuộc quyền (không chỉ ẩn CSS).
- Đăng ký mới không qua `next` cụ thể → mặc định vào `#/dat-lich`, KHÔNG vào `#/chon-anh` (khách mới chưa từng đặt lịch thì chưa có ảnh để xem); đăng nhập lại bằng tài khoản đã có (demo) vẫn vào `#/chon-anh` như cũ. Nếu đến từ một hành động cụ thể (vd click Album → `next=chon-anh`) thì tôn trọng `next`, không dùng mặc định.
- **Gộp 3 file khách hàng thành 1 (`index.html`, SPA nhỏ dùng `location.hash`):** người dùng chỉ muốn gộp phần Khách hàng, giữ riêng `login.html` (cổng vào chung, không thuộc riêng khách hàng) và `crm/admin.html` (đối tượng khác hẳn, đã tự gộp nội bộ trước đó rồi) — đã hỏi rõ phạm vi (3 file hay cả 5 file) trước khi làm, tránh gộp quá tay.
- **Chatbot AI: đổi từ Claude API sang Google Gemini API (2026-09-20):** người dùng làm bài tập về nhà, muốn dùng free tier của Gemini. Chỉ đổi phía `server/server.js` (endpoint `generativelanguage.googleapis.com`, header `x-goog-api-key`, format `contents`/`systemInstruction`, role bot là `model`, biến `.env` `GEMINI_API_KEY`/`GEMINI_MODEL`, model mặc định `gemini-3.1-flash-lite`, rẻ nhất trong bảng giá Google ngày 2026-09-20 ($0,25 vào / $1,50 ra mỗi triệu token; `3.5-flash-lite` là $0,30/$2,50): `gemini-2.5-flash` đã ngừng cho người dùng mới, còn `gemini-3.6-flash` chỉ cho 20 request ở free tier nên bị hết hạn mức ngay khi chạy test), frontend `js/script.js` không đổi. `maxOutputTokens` để 1024 (không phải 400 như bản Claude) vì model Gemini có thể tiêu token cho phần suy luận nội bộ và cắt cụt câu trả lời. Đã test thật: trả lời đúng system prompt và nhớ ngữ cảnh hội thoại. Dưới MỌI câu trả lời của AI hiện đúng 3 nút gợi ý do chính Gemini sinh cùng lúc (structured output JSON `{reply, suggestions:[{label, action}]}` qua `responseSchema`, server tự parse/lọc). Gợi ý có `action` (trang/mục website) thì bấm là chuyển thẳng tới đó, không gọi AI thêm (tiết kiệm hạn mức) và menu vẫn còn sau khi chuyển; gợi ý `action: none` thì gửi cho AI như khách gõ. AI lỗi hoặc không trả gợi ý thì dùng 3 gợi ý dự phòng (2 nút dẫn trang + 1 câu FAQ trả lời cục bộ). Server tự thử lại tối đa 2 lần khi Google báo 503/429 (free tier hay quá tải tạm thời). Test: `_screenshots/test-ai-faq-menu.js` (cần server chạy). **Chatbot chỉ dẫn khách đi khi khách BẤM gợi ý (người dùng chốt 2026-09-20, thay cho bản đầu "tự chuyển trang luôn"):** khách nhắn ý muốn ("đặt lịch", "xem ảnh của tôi"...) thì AI chỉ trả lời bằng chữ, KHÔNG tự chuyển trang; 1 trong 3 gợi ý bên dưới phải là nút dẫn tới đúng trang đó, bấm mới chuyển. Mỗi gợi ý có `action` (`dat-lich`, `chon-anh`, `dich-vu`, `concept`, `album`, `gioi-thieu`, `tin-tuc`, `trang-chu`, hoặc `none`), server lọc theo whitelist `ACTIONS`, frontend (`CHAT_ACTIONS`/`runChatAction` trong `js/script.js`) chuyển trang; mobile tự đóng khung chat để thấy trang đích. Lớp bảo đảm phía frontend `withIntentButton`: nếu khách nêu rõ ý (nhận diện bằng từ khoá `detectLocalAction`) mà AI quên nút tương ứng thì tự chèn lên đầu menu; câu hỏi thông tin ("quy trình đặt lịch thế nào?") thì không chèn. Nút chat nằm ngoài `<main>` nên luôn có ở cả 3 view khách hàng. Test: `_screenshots/test-suggestion-nav.js` (dùng phản hồi giả, không cần server, không tốn hạn mức, nên ưu tiên chạy cái này), `_screenshots/test-chat-navigate.js` (cần server chạy; tốn ~8 lượt gọi AI mỗi lần chạy, chú ý hạn mức free). `test-ai-chat.js`/`test-photos-chat.js` giờ tự chặn request tới `:3001` nên không phụ thuộc server bật hay tắt. Trước đó: người dùng đã chọn Claude API + server Node nhỏ chạy local (không deploy cloud lúc này) sau khi được báo xung đột với guardrail cũ. Guardrail đã cập nhật (xem bên dưới) để phản ánh đúng: quick-reply vẫn kịch bản, khung nhập tự do là AI thật khi server chạy.
- **Deploy public: chatbot AI dùng được từ mọi thiết bị, không chỉ máy chạy server (2026-09-23):** người dùng phát hiện chatbot báo lỗi "không kết nối được" khi mở lại máy hôm sau (nguyên nhân đơn giản: server Node là tiến trình riêng, không tự chạy cùng máy, phải tự `npm start` lại mỗi lần) — từ đó hỏi tiếp nếu public web thì máy khác/điện thoại có dùng AI được không. Trả lời: không, vì `CHAT_API_URL` cũ hardcode `localhost:3001` — mỗi trình duyệt tự hiểu `localhost` là chính nó, không phải máy chủ. Đã deploy thật: **server** (`server/`) lên Render (free tier, service `PhanTichWeb`, URL `https://phantichweb.onrender.com`, Root Directory `server`, Build `npm install`, Start `npm start`) — vướng 1 lỗi thật lúc làm: Start Command bị điền nhầm thành `npm install` (copy từ Build Command), server "Running" nhưng không lắng nghe cổng nào, phát hiện qua `curl -v` thấy connect được nhưng 0 bytes response (timeout), sửa lại `npm start` là hết; **frontend** (toàn bộ site tĩnh) lên GitHub Pages (`https://chungcchung1972005-cell.github.io/PhanTichWeb/`, nhánh `main`, thư mục `/root`) — vướng 1 bước: Pages free chỉ chạy được khi repo public, người dùng tự chuyển repo private → public (đã xác nhận an toàn vì `.env` chứa key thật chưa từng được commit, nằm trong `.gitignore` từ đầu). Code đã chuẩn bị sẵn 2 việc để chạy đúng cả 2 môi trường: (1) `js/script.js` `CHAT_API_URL` tự chọn theo `location.hostname`/`protocol` — mở qua `file://`/`localhost` thì gọi `http://localhost:3001`, mở từ nơi khác (đã public) thì gọi `PROD_CHAT_API_URL` (nay đã điền URL Render thật, trước đó tạm để placeholder `REPLACE-WITH-YOUR-RENDER-URL`); (2) `server/server.js` đọc whitelist CORS từ biến môi trường `ALLOWED_ORIGINS` (phân tách bằng dấu phẩy), để trống thì mở cho mọi origin như cũ kèm log cảnh báo (chỉ chấp nhận khi test local) — đã điền `https://chungcchung1972005-cell.github.io` trên Render, xác nhận bằng `curl -X OPTIONS` kèm header `Origin` khác nhau: domain đúng có `access-control-allow-origin`, domain lạ không có. Đã kiểm tra đầu-cuối thật trên `_screenshots/test-prod-chat.js` (mở đúng URL GitHub Pages bằng Puppeteer, không phải `file://`, chat thật với AI qua server Render, không lỗi CORS/console) — pass, có screenshot `prod-chat-live.png` xác nhận trực quan. Hướng dẫn deploy chi tiết từng bước (đã dùng để hướng dẫn người dùng thao tác trên dashboard Render/GitHub, các bước đó Claude không có quyền tự làm) nằm ở `server/DEPLOY.md`, vẫn giữ lại để tham khảo khi cần deploy lại hoặc đổi hosting khác. **Lưu ý duy trì:** Render free tier tự "ngủ" sau ~15 phút không có request, lần gọi đầu sau đó chậm 30-60s, không phải lỗi. Từ nay mọi thay đổi trong `server/` hoặc file gốc site tĩnh cần `git push` thì Render/GitHub Pages mới tự deploy lại — không cần lặp lại các bước cấu hình dashboard.
- **Quy trình chỉnh sửa ảnh chỉ 3 bước, KHÔNG có vai trò QC riêng:** người dùng xác nhận trực tiếp (2026-09-19) quy trình thật của Thợ ảnh là xác nhận yêu cầu → đang sửa ảnh → tải ảnh đã sửa lên là hoàn thành — không qua một bước/vai trò QC tách riêng để duyệt trước khi hoàn thành (dù tài liệu cũ từng mô tả vậy). Đã sửa `STATUS_FLOW` (`js/data-store.js`), kanban (`crm/admin.html`/`crm/js/admin.js`), và tài liệu (`rules/tech-defaults.md`, `rules/workflow.md`) cho khớp — xem log "Bỏ bước Chờ QC..." bên dưới. Nếu sau này người dùng muốn thêm lại 1 bước duyệt/QC riêng (vd khi có thêm nhân sự QC), đây sẽ là thay đổi ngược lại quyết định này, cần hỏi lại rõ ràng trước khi làm chứ không tự suy đoán.
- **[ĐÃ LỖI THỜI 2026-09-25: người dùng đã tự bật PR + branch protection cho `main`, xem "Ghi nhớ nhanh" ở đầu file]** Thảo luận quy trình Git/chia việc nhóm 3 người — CHƯA CHỐT, người dùng chủ động gác lại: người dùng hỏi có nên dùng Pull Request thay vì commit thẳng `main`, và nên chia việc theo hướng nào cho nhóm 3 người (đề xuất đã chuẩn bị: PR bắt buộc + chia theo khu vực chức năng Trang khách hàng/Admin-CRM/Nền tảng dùng chung, xem chi tiết trong plan file phiên này nếu còn `C:\Users\ADMIN\.claude\plans\distributed-wondering-kitten.md`) — nhưng người dùng chủ động nói "để sau đi, muốn demo tính năng trước". Không tự ý triển khai (tạo branch, bật branch protection...) khi chưa được xác nhận lại; nêu lại đề xuất nếu người dùng quay lại chủ đề này.
